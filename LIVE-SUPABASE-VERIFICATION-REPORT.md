# نَقْلة / NAQLA — Live Supabase Verification Report
**التاريخ:** 2026-09-26 · **الأمر المقصود:** `npm run live:supabase` · **الحالة:** لم يُنفَّذ

> **لم يُشغَّل أي فحص حيّ. لم يُستخدم أي بديل محلي.** بيئة التنفيذ لا تحوي المتغيرات المطلوبة، والسياسة الشبكية ترفض مضيفات Supabase. توقفتُ عند الفحص المسبق كما طُلب، ولم يُطبَّق أي تغيير على أي مشروع.

# Environment

| البند | النتيجة | الدليل |
|---|---|---|
| **Supabase connectivity** | ⛔ **مرفوض شبكيًا** | وكيل البيئة يسجّل `connect_rejected` لـ`supabase.com:443` و`api.supabase.com:443` (ثلاث محاولات، آخرها `2026-09-26T19:29:47Z`)؛ `curl` يعيد `000` |
| **database connectivity** | ⛔ **لم يُختبر** | `DATABASE_URL` غير مضبوط |
| **auth connectivity** | ⛔ **لم يُختبر** | `NEXT_PUBLIC_SUPABASE_URL` · `NEXT_PUBLIC_SUPABASE_ANON_KEY` · `SUPABASE_JWT_SECRET` غير مضبوطة |
| **storage connectivity** | ⛔ **لم يُختبر** | `SUPABASE_SERVICE_ROLE_KEY` غير مضبوط |

**المتغيرات المفقودة — بالاسم، الخمسة كلها:**
`NEXT_PUBLIC_SUPABASE_URL` · `NEXT_PUBLIC_SUPABASE_ANON_KEY` · `SUPABASE_SERVICE_ROLE_KEY` · `SUPABASE_JWT_SECRET` · `DATABASE_URL`
(لا `.env.local` في المستودع، ولا قيمة في بيئة العملية. لم تُطبع أي قيمة سرّية لأن لا قيمة موجودة.)

**الفحص المسبق (§1):** توقف عند البند الأول — لا يمكن تأكيد وصول المشروع، فلا هجرات ولا RLS ولا حاويات ولا أدوار يمكن فحصها. **لم يُطبَّق ولم يُعدَّل شيء.**

# PASS
لا شيء. لم يُثبَت أي بند على مشروع Supabase حقيقي.

# FAIL
لا شيء. لم يُنفَّذ أي فحص حيّ فلا نتيجة صحيحة أو خاطئة.

# BLOCKED

البنود الاثنا عشر المطلوبة كلها، وما بعدها:

| # | الفحص | الخطوة في `scripts/supabase-live-check.mjs` | الحاجب |
|---|---|---|---|
| 1 | المصادقة الحقيقية | 1–2 (`createUser` + `signInWithPassword`) | المتغيرات + الشبكة |
| 2 | إنشاء الجلسة | 1–2 | المتغيرات + الشبكة |
| 3 | تجديد الرمز | 4 (`refreshSession` ⇒ رمز جديد يقبله الـAPI) | المتغيرات + الشبكة |
| 4 | رفض الرمز المزوّر | 5 | المتغيرات + الشبكة |
| 5 | `anon` يطيع RLS | 6 (سبعة جداول خاصة بمفتاح `anon` الحقيقي) | المتغيرات + الشبكة |
| 6 | A لا يقرأ سجلات B | 9 + 12 | المتغيرات + الشبكة |
| 7 | A لا يعدّل سجلات B | 9 | المتغيرات + الشبكة |
| 8 | رفع خاص بالتدفق الموقّع | 7 (نية ⇒ `PUT` موقّع ⇒ تأكيد مع مطابقة `sha256` المقيسة) | المتغيرات + الشبكة |
| 9 | التنزيل الموقّع للمخوَّل فقط | 8 (رابط موقّع يعمل، والرابط العام الخام **لا** يخدم الملف) | المتغيرات + الشبكة |
| 10 | B لا يوقّع ولا يصل كائن A | 9 (`list` تحت بادئة A ⇒ صفر، `createSignedUrl` ⇒ لا رابط) | المتغيرات + الشبكة |
| 11 | رابط المشاركة يعمل حيًّا | 12 (المسار العام + قراءة `evidence_report` بمفتاح `anon` عبر RLS) | المتغيرات + الشبكة |
| 12 | الإلغاء يوقفه فورًا | 12 (`DELETE` ⇒ المسار مغلق **و**RLS تحجب الصف) | المتغيرات + الشبكة |
| — | الإسقاط العام بلا مرفوعات خام | 11–12 (فحص `object_path` · `bucket` · `signedUrl` · اسم الملف · `checksum`) | المتغيرات + الشبكة |
| — | `Verified` يبقى محجوبًا حيًّا | **غير مغطّى بالسكربت الحيّ** — مُثبت محليًا فقط (قيد `transition_verified_not_yet_available`) | + يحتاج خطوة إضافية |
| — | المستخدم لا يرقّي نفسه حيًّا | **غير مغطّى بالسكربت الحيّ** بكتابة مباشرة عبر PostgREST — مُثبت محليًا فقط | + يحتاج خطوة إضافية |
| — | عزل مسارات `service_role` | جزئي (الخطوة 8 تستعمل `admin` لقراءة المسار ثم تثبت أن الرابط العام لا يخدمه) | + يحتاج خطوة إضافية |

**فحوص التخزين (§3)** و**فحوص RLS بأدوار غير superuser (§4)**: السكربت يستعمل مفتاح `anon` ورموز مستخدمين حقيقية (السياق الفعلي: `anon` / `authenticated` عبر PostgREST وStorage API)، ويستعمل `service_role` **فقط** لإنشاء المستخدمين المؤقتين وحذفهم وقراءة مسار الكائن كي يثبت أن الرابط الخام لا يخدمه. **لم يُنفَّذ أيٌّ منها.**

# LOCAL-ONLY
كما في `VERTICAL-SLICE-01-1-HARDENING-REPORT.md` بلا تغيير: **٢١٩ فحصًا** على PostgreSQL 16 محلي وبديل تخزين ذاكري موسوم. السياق الفعلي هناك: `authenticated` و`anon` حقيقيان بحارس `assert_effective_role()` (لا `postgres` ولا `service_role` ولا superuser)، و`UPDATE` غير المسموح يُقبل PASS فقط بشرطَي «صفر صفوف» و«البيانات لم تتغيّر» — وهو ما يُختبر حرفيًا. **هذا لا يرقى إلى إثبات حيّ، ولم يُقدَّم بديلًا عنه.**

# SECURITY FINDINGS
لا يمكن تسجيل أي فرق بين PostgreSQL المحلي وSupabase الحقيقي، لأن الثاني لم يُنفَّذ. **ثلاث فجوات في تغطية السكربت الحيّ** رُصدت أثناء المراجعة (لا في المنتج): `Verified` محجوبًا، والترقية الذاتية عبر PostgREST، وعزل `service_role` — ستُضاف كخطوات قبل التشغيل الحيّ الأول. لم أضفها الآن التزامًا بألّا يُعدَّل شيء في هذه المهمة سوى التحقق.

# REQUIRED ACTIONS

فقط ما يلزم قبل الشريحة التالية:

1. **ضبط المتغيرات الخمسة في بيئة التنفيذ** (قائمة البيئة السحابية ← Edit ← API credentials أو متغيرات البيئة): `NEXT_PUBLIC_SUPABASE_URL` · `NEXT_PUBLIC_SUPABASE_ANON_KEY` · `SUPABASE_SERVICE_ROLE_KEY` · `SUPABASE_JWT_SECRET` · `DATABASE_URL`. **لا تُلصق في الدردشة.** جلسة جديدة تلتقطها.
2. **السماح الشبكي** لـ`supabase.com` و`api.supabase.com` و`*.supabase.co` (Network access في إعدادات البيئة، أو مستوى وصول أوسع).
3. ثم تُعاد هذه المهمة كما هي: فحص مسبق، `npm run live:supabase`، تقرير.

# FINAL STATUS

**LIVE SUPABASE VERIFICATION: BLOCKED**
