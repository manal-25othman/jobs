{
  "name": "@naqla/agents",
  "version": "0.0.0",
  "private": true,
  "description": "Agent contracts, gateway, deterministic orchestrator and specialist agents. Depends on @naqla/domain only. No model SDK.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "require": "./dist/index.js", "default": "./dist/index.js" } },
  "scripts": { "build": "tsc -p tsconfig.json", "test": "npm run build && node --test dist/**/*.test.js", "clean": "rm -rf dist" },
  "dependencies": { "@naqla/domain": "*" },
  "devDependencies": { "@types/node": "22.10.2", "typescript": "5.9.3" }
}
