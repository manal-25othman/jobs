{
  "name": "@naqla/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Design tokens and shared UI primitives. Empty in Phase 0 by design — no screen has been migrated.",
  "main": "./src/index.ts",
  "scripts": { "build": "echo '@naqla/ui has no build in Phase 0'" }
}
