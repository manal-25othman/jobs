# نَقْلة / NAQLA — Phase 0 Readiness Report
**الإصدار:** 1.0 · **التاريخ:** 2026-09-26 · **الحالة:** بانتظار اعتماد المالكة

> **ما هذا التقرير:** حكم صريح على كل بوابة خروج من الاثنتي عشرة، بدليل قابل لإعادة التشغيل.
> **ما ليس:** ادعاء جاهزية إنتاج. Phase 0 أساسات، وما يليها هو المنتج.

---

## ملخص البوابات

| # | البوابة | الحكم |
|---|---|---|
| 1 | بنية المستودع ومساحة العمل | ✅ **PASS** |
| 2 | إقلاع التطبيقين الإنتاجيين | ✅ **PASS** |
| 3 | حزمة نطاق بلا استيراد إطار | ✅ **PASS** |
| 4 | اختبارات آلية للثوابت | ✅ **PASS** |
| 5 | توثيق نموذج النطاق | ✅ **PASS** |
| 6 | مخطط قاعدة بيانات أولي | ✅ **PASS** |
| 7 | نموذج الوصول/RLS موثَّق ومُختبَر | ✅ **PASS** |
| 8 | حدود الذكاء الاصطناعي موثَّقة | ✅ **PASS** |
| 9 | عقود API للشريحة الأولى | ✅ **PASS** |
| 10 | `.env.example` والتحقق من الإعدادات | ✅ **PASS** |
| 11 | توثيق التطوير المحلي | ✅ **PASS** |
| 12 | **لم يبدأ نقل أي شاشة إنتاجية** | ✅ **PASS** |

**١٢ / ١٢ PASS · صفر FAIL · صفر BLOCKED على البوابات نفسها.**

> **لكن ثلاثة حواجب خارج البوابات ما زالت قائمة** (§٣ أدناه)، واثنان منها يمنعان الإطلاق لا البناء. وبوابةٌ خضراء لا تعني منتجًا جاهزًا.

---

## ١. تفصيل البوابات

### ١ · بنية المستودع ومساحة العمل — ✅ PASS
```
apps/web (مُجمَّد) · apps/app (Next.js) · apps/api (NestJS)
packages/domain · contracts · config · ui
supabase/migrations · supabase/tests · scripts · docs/architecture
```
مساحة عمل npm واحدة. **اتجاه الاعتماد مفروض آليًا**: `domain → contracts → (api · app)`.
**الدليل:** `npm run verify:boundaries` ⇒ *"Architecture boundaries hold"*.

### ٢ · إقلاع التطبيقين — ✅ PASS
| التطبيق | الدليل |
|---|---|
| NestJS | `nest build` ينجح · العملية تقلع · `GET /health` ⇒ `{"status":"ok"}` · `GET /ready` ⇒ `degraded` مع `database: not_wired` |
| Next.js | `next build` ينجح · صفحتان ثابتتان · الصفحة تعرض قيمًا **مستوردة من `@naqla/domain`** |

**`/ready` يقول `degraded` عمدًا.** لا قاعدة ولا تخزين ولا مصادقة موصولة، وعلامة خضراء كاذبة أسوأ من حمراء صادقة.

**وأُثبت مسار الفشل أيضًا**: بإعدادات ناقصة يرفض الـAPI الإقلاع ويطبع **كل** المشكلات الخمس دفعة واحدة.

### ٣ · حزمة نطاق بلا إطار — ✅ PASS
`packages/domain` (١١ وحدة) لا يستورد إلا `node:` builtins ونفسه.
`verify-boundaries.mjs` يفحص أربعة أشياء:
1. نقاء النطاق · 2. نقاء العقود · 3. **انزياح القواعد**: إعلان اسم قاعدة داخل تطبيق **يفشل البناء** · 4. عدم استيراد النموذج المُجمَّد من كود إنتاجي.

**البرهان الحي:** `GET /v1/domain/evidence-model` يخدم الحالات والانتقالات **مقروءة من الحزمة**، ولا يعيد كتابتها. تغيير قاعدة في النطاق يغيّر الاستجابة تلقائيًا.

### ٤ · اختبارات الثوابت — ✅ PASS

**طبقتان، كل منها كافية وحدها:**

| الطبقة | العدد | الدليل |
|---|---|---|
| TypeScript | **٦٣ اختبارًا** (+٧ للإعدادات) | `npm test` |
| PostgreSQL | **٢٩ إثباتًا** | `scripts/db-test.sh` |

**لكل ثابت من التسعة حالة ناجحة وحالة مخالِفة تفشل صراحةً** بالرمز الصحيح — لا قيمة خاطئة يمكن تجاهلها.

مع تغطية إضافية لما طلبتِه بالاسم: الادعاءات غير المدعومة · ترقية الدليل · الصياغة المولّدة · المشاركة العامة · أهلية التقرير · عدم قابلية التقييمات للتعديل.

> **اختبار كشف خطأ نمذجة حقيقيًا:** سقط اختبار «لكل حالة عقد عرض مميّز» لأني كنت قد سوّيتُ بين `gap` و`self_reported`. لكن جدول §8 يفرّق: مهارة مُعلنة ذاتيًا **موجودة في السيرة تُوسَم**، والفجوة لا شيء يُوسَم. **صُحِّح النطاق لا الاختبار**، وأُضيف حقل `flagIfPresentInCv`. وسُجِّل القرار في D-050.

### ٥ · توثيق نموذج النطاق — ✅ PASS
`docs/architecture/DOMAIN-MODEL.md` — **٢٩ كيانًا** مطلوبًا، ولكلٍّ: الغرض · المُعرِّف · الملكية · دورة الحياة · الحقول المهمة · العلاقات · الثابت مقابل المتغيّر · تصنيف الخصوصية.

**وثلاثة كيانات لم تُفرَد بجداول، مع تبرير مكتوب** بدل تنفيذ صامت:
- `Skill Requirement` مُجسَّد في `role_requirement` — إفراده يُنتج جدولًا بحقل واحد وعلاقتين.
- `Project Evidence` مُجسَّد في `evidence.project_id` — «دليل المشروع» ليس نوعًا آخر من الدليل، وإفراده يخلق مسارين لنفس الحقيقة.
- `Evidence Source` قيمة مُعدَّدة على `Evidence` لا جدول.

**ولم يُخترع حقل لراحة الواجهة.** كل حقل بجانبه سبب: متطلب أو ثابت أو ضرورة تشغيلية.

### ٦ · مخطط قاعدة البيانات — ✅ PASS
`0001_init.sql` · **٤٢ جدولًا**، **مطبَّقة فعليًا** على PostgreSQL 16.13 لا مكتوبة فقط.

| المطلوب | الحالة |
|---|---|
| مُعرِّفات UUID | ✅ في كل جدول |
| طوابع زمنية | ✅ `created_at` في الكل · `updated_at` بمُحفِّز في كل قابل للتعديل |
| ملكية صريحة | ✅ `user_id` مباشر حتى حين يمكن اشتقاقه — **قرار أمني**: سياسة RLS على عمود مباشر فحص مساواة، وعبر وصلات استعلام فرعي يخضع بدوره لـRLS |
| مفاتيح أجنبية للثوابت | ✅ INV-1 مفتاح + قيد، لا شرط برمجي |
| تقييمات تاريخية بالإضافة فقط | ✅ `REVOKE UPDATE, DELETE` + `supersedes_result_id` بفهرس فريد |
| أحداث التدقيق لا تُكتب فوقها | ✅ نفس المعاملة |
| حذف ناعم حيث يلزم فقط | ✅ أربعة مواضع، لكلٍّ سبب · و`notification` تُحذف فعليًا |

**`jsonb` في ستة أعمدة فقط، ولكلٍّ تبرير مكتوب**، ولا كيان نطاق أساسي. دراسة الحالة أقسامها **أعمدة** لأن الأقسام الأربعة قاعدة منتج: لو كانت `jsonb` لأمكن نشر دراسة بثلاثة أقسام.

**INV-9 يستحق وقفة:** لا يُفرض بقيد بل **بغياب عمود**. لا عمود على `skill_claim` يشتق من درجة نشاط — لأن تصميمًا يمنع الخطأ أقوى من قيد يرصده.

### ٧ · نموذج الوصول وRLS — ✅ PASS
`0002_rls.sql` + `ACCESS-CONTROL-MODEL.md` + `supabase/tests/rls.sql`.

**٤٢ من ٤٢ جدولًا** بـ`enable` + **`force`** · **٤٦ سياسة** · المنع هو الافتراض.
مُختبَر بثلاثة أدوار حقيقية: `authenticated` (مستخدمان) و`anon`.

**أهم ما أُثبت — المستخدم لا يستطيع ترقية نفسه:**
```
user A cannot write their own skill claim        → denied by RLS
user A cannot write their own evidence           → denied by RLS
user A cannot promote their own evidence state   → denied by RLS
user A cannot rewrite an evaluation result       → permission denied
user A cannot delete ladder history              → permission denied
user A cannot set their own readiness score      → denied by RLS
```

**ودورة رابط المشاركة كاملة:** بلا رابط ⇒ مغلق · برابط حي ⇒ مفتوح · **بعد الإلغاء ⇒ مغلق فورًا** · منتهٍ ⇒ مغلق.
**ونشر الملف العام لا يكشف** الأدلة خلفه ولا الملفات المرفوعة.

> **خطأ اختبار وجدته وصحّحته، وأذكره لأن إخفاءه يُفسد قيمة هذا التقرير:**
> النسخة الأولى استعملت `SET LOCAL` خارج معاملة — وهو **أمر بلا أثر**. فمرّ الاختبار كاملًا وهو يعمل بصلاحية superuser التي **تتجاوز RLS أصلًا**، أي أنه كان يثبت لا شيء.
> صُحِّح إلى `SET ROLE`، **وأُضيف حارس `assert_effective_role()` يفشل الاختبار** إن لم يتبدّل الدور فعلًا أو كان يتجاوز RLS. الحارس موجود تحديدًا لمنع تكرار هذا الصنف من النجاح الكاذب.

**والتخزين:** أربع حاويات كلها `public = false` · مسار الكائن مسبوق بمعرّف صاحبه فلا يُعدَّد مجلد غيره · روابط موقّعة ٦٠ث للرفع و٣٠٠ث للتنزيل، **لأن الرابط طويل العمر اعتماد مشاركته لا يستطيع أحد إلغاءه**.

### ٨ · حدود الذكاء الاصطناعي — ✅ PASS
`AI-BOUNDARY.md`: ما يجوز (شرح · صياغة مقترحة · تلخيص · مسودة دراسة حالة · توصيات) مقابل ما لا يجوز (اختلاق دليل · ترقية · وسم `Demonstrated`/`Verified` · تعديل تاريخ · نشر ادعاء · تجاوز موافقة).

**ثلاث طبقات منع، كل منها كافية:** نوع TypeScript (`aiMayExecute: false` **قيمة حرفية**) · حارس نطاق (INV-3) · قيد قاعدة (`transition_actor_is_not_agent`).

**عقد تجريد المزوّد مكتوب** حول **قدرات** لا اسم مزوّد — لأن التجريد المصمَّم على «القاسم المشترك الأدنى» يُفقد مزايا المزوّد، وهو الخطر الحقيقي في هذه الطبقة.

**والحالة اليوم: صفر استدعاءات، صفر مفاتيح** — **واختبار يفشل** إن أُضيف مفتاح مزوّد (D-049).

### ٩ · عقود API — ✅ PASS
`packages/contracts` يعتمد على `@naqla/domain` **فقط**، فلا يدرج عقد عن قاعدة بلا خطأ نوع.

- **مغلّف الاستجابة**: كل جسم إما `{ok:true,data,meta}` أو `{ok:false,error,meta}`. **لا حمولات عارية.**
- **نموذج الخطأ**: ١٢ رمزًا مغلقًا + خريطة إلى HTTP. و`invariant` يظهر في الخطأ ليشرح **أي قاعدة** منعت العملية لا فشلًا عامًّا.
- **الإصدار**: `/v1/` في المسار، بقاعدة صريحة لما يستوجب الرفع وما لا يستوجبه، ونافذة تعايش.
- **الشريحة الأولى**: **١٥ نقطة نهاية معرَّفة، صفر منفَّذة** — كما طُلب.
- **`ScoreDto` يحمل التنويه إلزاميًا** فلا تعبر درجة الشبكة بلا تنويهها.

### ١٠ · الإعدادات — ✅ PASS
`.env.example` **مولَّد من `ENV_SPEC`** — مصدر واحد، فلا ينحرف الملف عن الكود.
التحقق يفشل مبكرًا ويطبع **كل** المشكلات دفعة واحدة.

**وثلاثة حراس مُختبَرة:**
- **سر في متغير `NEXT_PUBLIC_` مرفوض** — يمنع أخطر تسريب في هذا الـStack.
- **`NAQLA_DEPLOY_REGION` إلزامي** — لأن D-004 يجعل موطن البيانات قرارًا موثّقًا، ومنطقة بلا اسم لا تُوثَّق.
- **لا مفتاح مزوّد نماذج** — اختبار يفشل إن أُضيف.

### ١١ · التطوير المحلي — ✅ PASS
`LOCAL-DEVELOPMENT.md`: المتطلبات · أول تشغيل · الأوامر · القاعدة بطريقتين · خريطة المستودع · قواعد العمل · **مشكلات شائعة** — بما فيها فخ `SET LOCAL` الذي وقعتُ فيه.

**ولا حاجة إلى Docker** لتشغيل النطاق واختباراته: أرخص حلقة تغذية راجعة يجب ألّا تحتاج حاوية.

### ١٢ · لم يبدأ نقل الشاشات — ✅ PASS
| ما طُلب عدم فعله | الحالة |
|---|---|
| نقل الشاشات المُجمَّدة | ⛔ **لم يبدأ** — صفحة واحدة تعرض قيمًا مستوردة، بلا مكوّن من التصميم |
| تدفّقات المصادقة | ⛔ **لم تُنفَّذ** |
| المُقيِّم | ⛔ **لم يُنفَّذ** |
| استدعاءات AI | ⛔ **لم تُنفَّذ** — ولا مفتاح |
| بناء السيرة | ⛔ **لم يُنفَّذ** |

**والنموذج المُجمَّد سليم**: `FROZEN-MANIFEST.json` ببصمة **١٨ ملفًا**، و`verify:prototype` يفشل عند أي تغيير.
**و`@naqla/ui` فارغة عمدًا**: نقل الرموز الآن يُنتج مصدرَي حقيقة لنفس القيم، يبدآن بالانحراف فور تغيّر أحدهما.

---

## ٢. كيف يُعاد التحقق من كل ما سبق

```bash
npm install
npm test                     # 63 + 7 اختبارًا
npm run verify:boundaries    # نقاء النطاق وعدم انزياح القواعد
npm run verify:prototype     # سلامة النموذج المُجمَّد
npm run build -w @naqla/api  # بناء NestJS
npm run build -w @naqla/app  # بناء Next.js
DATABASE_URL=postgres://... scripts/db-test.sh   # 29 إثبات ثابت + إثباتات الوصول
```

---

## ٣. ما زال محجوبًا — خارج البوابات

| # | الحاجب | يمنع | الإجراء |
|---|---|---|---|
| 1 | **`OPEN-022`** موطن البيانات ومزوّد الاستضافة | **الإطلاق، لا البناء** | التوثيق **يسبق** الاختيار (D-004). D-045a يُبقي الـAPI بحاوية مستقلة، و`NAQLA_DEPLOY_REGION` إلزامي |
| 2 | **`OPEN-023`** شروط معالجة البيانات لدى مزوّد النماذج | **أي استدعاء نموذج** | مراجعة الشروط قبل أول استدعاء (D-049) |
| 3 | **اعتماد SME** | أي نشاط يمنح `Verified` | حزمة Frontend كلها `draft` و`can_yield_verified = false` — **وهذا صحيح ومقصود** · القيد مفروض في القاعدة (D-030) |
| 4 | **`OPEN-016`** روابط دراسات الحالة العامة | نشر الملف العام | القراءة المتحفظة مفروضة الآن: كل رابط ينتهي، ولا فهرسة |
| 5 | **`OPEN-017`** تبريد `username` | حذف الحساب وإعادة التسجيل | `username_release` يسجّل وقت التحرير فتُطبَّق السياسة لاحقًا بلا إعادة بناء التاريخ |

---

## ٤. قرارات تنتظر اعتمادك

| # | البند | ما فعلتُه مؤقتًا | ما أحتاجه |
|---|---|---|---|
| **1** | **`OPEN-EVAL-01` — أي «حالات خمس» قصدتِ؟** | حفظتُ **مجموعتين**: `EvaluationOutcome` (خمس، **مُشتقّة** من مراحل doc 11 ولم ترد مرقّمة) و`VerificationOutcome` (خمس، **منصوصة صراحةً** في `docs/blueprint/21` §5) | تأكيد أيّهما المقصود، أو إقرار الاثنتين. **دمجهما كان سيخلط «كيف انتهى التقييم» بـ«ماذا قررت المراجعة»** |
| **2** | **أوزان الدرجات** | `loadWeights()` **يفشل** حتى تُعتمد (D-048) · المفاتيح معتمدة من §9، الأوزان لا | متى تُعتمد؟ اقتراحي: بعد Phase 0، من بيانات حقيقية لا من تقدير |
| **3** | **NestJS مع Supabase** | بُني كما قرّرتِ | **ملاحظة لا اعتراض**: Supabase يقدّم PostgREST وRLS، فجزء من الـAPI سيكرّر ما تقدّمه المنصة. القيمة الحقيقية للـAPI هي **ما لا تستطيع RLS فعله**: خط التقييم، اشتقاق الأصول، البوابة، الطابور. اقتراحي أن تقرأ الواجهة **مباشرةً عبر RLS** حيثما كانت العملية قراءة صرفة، ويحتفظ الـAPI بما يحمل قواعد. القرار قرارك |
| **4** | **`@naqla/ui` فارغة** | نقل الرموز مؤجَّل لمرحلة النقل | تأكيد أن هذا ما تريدينه: **مصدر حقيقة واحد للرموز** حتى لحظة النقل |
| **5** | **الشريحة الأولى** | ١٥ عقدًا معرَّفًا، صفر منفَّذ | إذنك بالبدء — **والبدء يعني تنفيذ المسار كاملًا رأسيًا**، لا بناء كل الصفحات |

---

## ٥. الحكم

**Phase 0 مكتملة: ١٢/١٢ بوابة PASS.**

**ما أُثبت فعلًا:** أن القواعد قابلة للتعبير مستقلةً عن أي إطار، وأنها مفروضة في مكانين لا واحد، وأن المستخدم **لا يستطيع ترقية نفسه** — وهذا أهم ما في المنتج كله، وقد أُثبت بمحاولة فاشلة لا بادعاء.

**ما لم يُبنَ:** المنتج. لا مصادقة، ولا onboarding، ولا تقييم، ولا درجات، ولا شاشة واحدة منقولة.

**الخطوة التالية المقترحة:** الشريحة الرأسية الأولى كاملة —
`مستخدم → هدف → مشروع → تسليم → تقييم → تحديث حالة الدليل → أصل مهني → تقرير`
وهي **أهم من بناء كل الصفحات**، لأنها تختبر المعمارية من طرفها إلى طرفها بدل اختبار عرضها.

**ولم يبدأ أي نقل للشاشات. بانتظار اعتمادك.**
