# نَقْلة / NAQLA — Vertical Slice 1 Report
**الإصدار:** 1.0 · **التاريخ:** 2026-09-26 · **الحالة:** بانتظار مراجعة المالكة

> **المسار المُثبَت:**
> `الهدف المهني → المشروع → تسليم الدليل → التقييم → تحديث حالة الدليل → أصل مهني → تقرير الأدلة`
>
> **ولا استدعاء نموذج لغوي في أي خطوة منه.**

---

# PROVEN — ما أُثبت بتشغيل فعلي

> كل بند هنا مدعوم باختبار يعمل. الأرقام محسوبة من تشغيل حقيقي لا مقدَّرة (D-043b).

## نتائج الاختبارات

| الطبقة | العدد | الأمر |
|---|---|---|
| قواعد النطاق | **٩٤ ناجحًا / صفر فاشل** | `npm test` |
| الإعدادات | **٧ ناجحة / صفر فاشل** | `npm test` |
| ثوابت قاعدة البيانات | **٢٩ إثباتًا / صفر فشل** | `scripts/db-test.sh` |
| نموذج الوصول (RLS) | **٤٢ إثباتًا / صفر فشل** | `scripts/db-test.sh` |
| **طرف إلى طرف** | **٢٩ ناجحًا / صفر فاشل** | `npm run test:e2e -w @naqla/api` |
| **المجموع** | **٢٠١ فحصًا، صفر فشل** | |

بناء نظيف: `@naqla/domain` · `@naqla/contracts` · `@naqla/config` · `@naqla/api` (NestJS) · `@naqla/app` (Next.js، ٨ مسارات).

## الثلاثة عشر إثباتًا المطلوبة

| # | المطلوب | الحالة | الدليل |
|---|---|---|---|
| 1 | المستخدم يُنشئ/يختار هدفًا مهنيًا | ✅ | `PUT /v1/me/career-goal` — والتأكيد **صريح**؛ `confirmed: false` مرفوض بـ`400` |
| 2 | المستخدم يُنشئ مشروعًا | ✅ | نشاط منصة يسجّل نسخة مواصفته؛ نشاط بلا مواصفة مرفوض (INV-7) |
| 3 | المستخدم يُسلّم دليلًا | ✅ | التسليم **يُقفَل** ويسجّل وقت القفل، ويحمل الإقرار والوقائع المهيكلة |
| 4 | **الدليل المُسلَّم لا يرقّي نفسه** | ✅ | الادعاء يصل `practiced` فقط · `evidenceCount = 0` · **صفر صفوف في `evidence`** قبل التقييم |
| 5 | التقييم يُنفَّذ مقابل الرُبريك | ✅ | حتمي بالكامل · أربعة معايير · كل درجة بمبرر مكتوب |
| 6 | **المعايير الساقطة لا تُنشئ انتقالًا** | ✅ | `below_threshold` ⇒ `transition = null` · صفر أدلة · الادعاء يثبت عند `practiced` |
| 7 | التقييم الناجح ينقل `Practiced → Demonstrated` | ✅ | `T-DEMO-FROM-PRACTICED` · دليل واحد · حالة `demonstrated` |
| 8 | **تاريخ التقييم غير قابل للتعديل** | ✅ | `UPDATE`/`DELETE` ⇒ `permission denied` على النتائج والمعايير والانتقالات وسجل التدقيق · وإعادة تقييم نفس التسليم مرفوضة لا مستبدَلة |
| 9 | بند السيرة يُولَّد من دليل مدعوم فقط | ✅ | من `demonstrated` فقط · `practiced` و`self_reported` يرميان · **كل جملة تتبع واقعة** |
| 10 | **الاعتماد الصريح شرط للتفعيل** | ✅ | `approved: false` ⇒ لا تفعيل · `lifecycle_state` يبقى غير `active` · وقيد القاعدة يرفض `active` بلا اعتماد |
| 11 | التقرير من الحقول المسموحة فقط | ✅ | قائمة بيضاء · **وفحص يرفض أي مفتاح ممنوع على أي عمق** |
| 12 | **مستخدم ثانٍ لا يصل شيئًا** | ✅ | مشروع وتسليم وتقييم وأصل وتقرير ⇒ `404` · القوائم فارغة · ولا يُنشئ رابط مشاركة لتقرير غيره |
| 13 | الرابط العام يكشف الإسقاط وحده | ✅ | بلا رابط ⇒ مغلق · رمز خاطئ ⇒ مغلق · برابط حي ⇒ مفتوح · **بعد الإلغاء ⇒ مغلق فورًا** |

## ما لا يستطيعه المستخدم — وهو جوهر المنتج

مُختبَر من **طرفين مستقلين**: من واجهة الـAPI، ومن قاعدة البيانات مباشرةً بدور `authenticated` حقيقي.

```
insert into skill_claim ... 'verified'        → row-level security
insert into evidence ...                      → row-level security
insert into evidence_transition ... practiced→demonstrated  → row-level security
update evaluation_result ...                  → permission denied
delete from evidence_transition               → permission denied
delete from audit_event                       → permission denied
insert into readiness_score ...               → row-level security
```

**الحد الذي طلبتِه صراحةً مفروض بثلاث طبقات مستقلة:**
`Gap / Self-reported / Practiced → Demonstrated / Verified` **لا يمكن بكتابة مباشرة** —
(١) لا سياسة RLS تسمح، (٢) قيود القاعدة ترفض، (٣) حارس النطاق هو المسار الوحيد في الكود.

## سلامة المعاملة

الثلاث حالات التي طلبتِ استحالتها **مُختبَرة بالاستعلام عن وجودها، ولا تُوجد**:

| الحالة المستحيلة | الاستعلام | النتيجة |
|---|---|---|
| «التقييم اجتاز ولم يُنقل الدليل» | ترقية مقبولة بلا صف دليل | **صفر** |
| «انتُقل الدليل وتاريخ التقييم مفقود» | دليل بقوة منصة بلا `evaluation_result_id` | **صفر** |
| «أصل بلا دليل مصدره» | بند سيرة بلا `asset_evidence` | **صفر** |

التقييم والتحقق والانتقال وإنشاء الدليل وحدث التدقيق **كلها في معاملة واحدة** (`DbService.asService`).
**ولم يُستخدم طابور ولا outbox**: لا عمل غير متزامن في هذه الشريحة، وإضافتهما الآن بنية موزّعة بلا حاجة.

## لا ذكاء اصطناعي — مُثبت لا مُدَّعى

| الفحص | النتيجة |
|---|---|
| جدول `model_call` بعد كل تشغيل e2e | **فارغ** |
| اعتمادية مزوّد نماذج في أي `package.json` | **لا شيء** |
| مفتاح مزوّد في مخطط البيئة | **لا شيء** — واختبار **يفشل** إن أُضيف |
| تنفيذ صوري لبوابة الذكاء الاصطناعي | **لم يُضَف** (D-056) |

المُقيِّم يقارن وقائع مهيكلة بمعايير منشورة. وبند السيرة قالب يجمع عبارات من التقييم. **نفس المدخلات تعطي نفس المخرجات دائمًا** — مُختبَر بمقارنة تشغيلين.

## ما يرفضه المولّد

| الخطر | الحماية | مُختبَر |
|---|---|---|
| إطار مستنتَج | التقنيات تظهر **فقط** إن صرّح بها المستخدم | ✅ لا `React` في المخرج رغم أن المنصة مبنية به |
| مقياس مخترع | القالب **لا يُخرج نسبة مئوية أبدًا** | ✅ |
| لغة إتقان | «أتقنت» · «خبيرة» · `expert` · `mastered` مرفوضة | ✅ |
| جملة بلا مصدر | كل عبارة صف في `asset_trace` | ✅ الآثار = ٣ ثابتة (مشروع · مهارة · نتيجة) + واحد لكل معيار مستوفى + التقنيات إن صُرّح بها |

---

# DEMO / FIXTURE — يعمل، وليس بيانات إنتاج

| البند | ما هو | لماذا لا يُعدّ إنتاجًا |
|---|---|---|
| **دور Frontend Developer** | `supabase/seed/0001_demo_role.sql` · موسوم `is_demo_fixture = true` | `review_status = 'draft'` · والواجهة تعرض «**البيانات غير مكتملة بعد**» ولا تخترع متطلبات |
| **النشاط والرُبريك** | `act_fe_003@0.2.0` · `rub_fe_003@0.2.0` | `can_yield_verified = false` — **لا اعتماد SME** (D-030)، وقيد القاعدة يفرضه |
| **نسب الطلب السوقي** | **`NULL` عمدًا** | لا عيّنة إعلانات حقيقية خلفها، و**INV-4 يرفض رقمًا سوقيًا بلا مصدر ومؤرَّخ**. «مطلوبة في ٧ من ١٠» هنا كانت ستكون إحصاءً مخترعًا |
| **وقائع التسليم** | مربّعات اختيار في الواجهة | التسليم الحقيقي يرفع ملفات · **شكل الواقعة نفسه** في الحالتين، فالخط لا يتغيّر |
| **Supabase Auth** | ✅ الكود يتحقق من رمز Supabase حقيقي (HS256، مطالبة `sub`) — **والحارس المُختبَر هو الحارس الإنتاجي** | ⚠️ **لم تُشغَّل على مشروع Supabase حيّ**: لا Docker في بيئة البناء. **غير مُثبت:** التسجيل والبريد وتجديد الرمز |
| **Supabase Storage** | ⛔ **لم يُستخدم** | التسليم في هذه الشريحة وقائع مهيكلة لا ملفات. الحاويات مُعرَّفة في `0002_rls.sql` ولم تُختبر |
| **الرموز البصرية** | نسخة موسومة من النموذج المُجمَّد | `npm run verify:tokens` يفشل عند أي انحراف · **تُنقل** إلى `@naqla/ui` لاحقًا لا تُنسخ ثالثة |

---

# NOT IMPLEMENTED — لم يُبنَ عمدًا

## خارج نطاق الشريحة بقرارك

بنّاء السيرة · تحسين لينكدإن · مركز التعلّم · موقع المحفظة · التنبيهات الكاملة · تنسيق الوكلاء · بقية الشاشات — **لم يُلمس أيٌّ منها**.

## شاشات مُجمَّدة لم تُنقل — ولماذا

| الشاشة | لماذا لا |
|---|---|
| الرئيسية (N0) | تعرض ثلاث نسب جاهزية ومشاريع ورفيقًا وشبكة مهارات — **لا شيء منها في هذه الشريحة**. نقلها يعني عرض بيانات غير موجودة |
| مساري (N1) · المشاريع (N2) · المهارات (N4) | تعرض تقدّمًا وفجوات وترتيبًا بالأثر — يحتاج أوزانًا غير معتمدة |
| الملف المهني (N5–N9) | نسب جاهزية ومراجعة مسؤول توظيف — **خارج معايير القبول صراحةً** |
| `states.html` | مرجع تنفيذي لا شاشة مستخدم |

**نُفِّذت خمس شاشات فقط**: `/goal` · `/project` · `/evaluation` · `/asset` · `/report` (+ `/login`).
**الأقل يكفي**: دُمجت «المهمة» و«التسليم» في شاشة واحدة لأن فصلهما كان سيضيف تنقّلًا بلا قيمة.

## داخل المسار ولم يلزم

| البند | لماذا |
|---|---|
| **رفع الملفات** | الوقائع المهيكلة تُثبت الخط. الرفع يضيف فحص برمجيات خبيثة وروابط موقّعة ودورة حياة — كلها حقيقية ولا يحتاجها إثبات الخط |
| **ترقية `Verified`** | تحتاج مراجعة بشرية شرعية على مشروع ثانٍ، **وبوابة SME غير مستوفاة**. القواعد موجودة ومُختبَرة؛ **المسار لم يُنفَّذ بقرارك** |
| **طابور المهام** | لا عمل غير متزامن. الجدول موجود من Phase 0 ولم يُستعمل |
| **PDF** | التقرير يعمل كصفحة ويب. التصدير ليس تافهًا (خطوط عربية، RTL) فلم يُضَف |
| **نسب الجاهزية** | D-052 · وخارج معايير القبول |

---

# BLOCKED — محجوب بقرار سابق

| # | الحاجب | يمنع | لا يمنع |
|---|---|---|---|
| 1 | **`OPEN-022`** موطن البيانات | **الإطلاق الإنتاجي** | البناء · `NAQLA_DEPLOY_REGION` إلزامي والـAPI بحاوية مستقلة |
| 2 | **`OPEN-023`** شروط مزوّد النماذج | **أي استدعاء نموذج** | هذه الشريحة كلها — لا AI فيها أصلًا |
| 3 | **اعتماد SME** | `Verified` من أي نشاط | `Demonstrated` — مُثبت ويعمل |
| 4 | **`OPEN-016`** روابط عامة | فهرسة الروابط | المشاركة المؤقتة — تعمل بالقراءة المتحفظة |
| 5 | **أوزان الجاهزية** | أي نسبة جاهزية | الشريحة لا تحتاجها |
| 6 | **Docker/Supabase محليًا** | التحقق على حزمة حيّة | كل ما عداه — الحارس الإنتاجي مُختبَر برمز مطابق |

---

# التعارضات بين التصميم المُجمَّد والسلوك الإنتاجي

> **لم يُعدَّل التصميم لإخفاء أيٍّ منها.**

| # | التصميم يقول | الإنتاج يفعل | القرار |
|---|---|---|---|
| **1** | شاشة المهمة تعرض **«التقدّم ٤٠٪ · مخرج من ثلاثة»** | التقدّم مربّعات اختيار بلا نسبة | **النسبة مشتقة من مخرجات مُسلَّمة فعليًا**، ولا وجود لها قبل التسليم. عرض «٤٠٪» قبل أن يوجد ما يقيسه اختراع. **يحتاج قرارك**: أنُضيف عدّادًا حقيقيًا للمخرجات، أم تبقى المربّعات؟ |
| **2** | «عند التسليم يُقيَّم على **٤ معايير خلال دقائق**» | التقييم **فوري ومتزامن** | «خلال دقائق» تصف مُقيِّمًا بنموذج لغوي. الحتمي يُنهي في أجزاء من الثانية. **نص الواجهة عُدِّل** إلى «يُقيَّم على ٤ معايير — بلا نموذج لغوي». **تعارض نصّي مقصود ومعلن** |
| **3** | «المدير الافتراضي **ريم · مديرة المنتج**» ورسالتها | **لم يُنفَّذ** | شخصية افتراضية تحتاج محتوى مؤلَّفًا أو نموذجًا. الأول غير موجود والثاني ممنوع. **مؤجَّل بوضوح لا محذوف** |
| **4** | شاشة الملف تعرض **«مراجعة بعين مسؤول توظيف»** بأربع ملاحظات | تقرير الأدلة يعرض **ما أُثبت فقط** | المراجعة تحتاج مقارنة بإعلانات الدور وأوزانًا. الموجود أضيق وأصدق: كل سطر يتبع تقييمًا |
| **5** | الرئيسية تعرض **«مهاراتك نحو الدور»** بأربع مهارات | الفجوات لا تُعرض | الفجوة تحتاج **متطلبات دور مُراجَعة**، والفيكسشر `draft`. عرضها كان سيقدّم بيانات تجريبية كأنها مُصادق عليها |
| **6** | «تحقق قصير» يرفع المهارة إلى `Demonstrated` | ✅ **مطابق** | العنصر الوحيد من الوعد الأساسي الذي نُقل كما هو ويعمل |
| **7** | زر «**تطبيق التحسينين**» | «**معاينة** ← اعتماد» | نفس الانحراف المعلن في تنفيذ التصميم: §9 يمنع تسمية الأزرار «تطبيق» ويمنع تطبيق صياغة مولّدة بلا اعتماد. **القاعدة رُجّحت، وينتظر تأكيدك** |

## تعارض بنيوي وجدته أثناء البناء

**التصميم يعرض «فجوة» كحالة في شريط الدرجة.** لكن الفجوة **غياب** ادعاء لا حالة مخزَّنة: لو خُزِّنت لاحتاج كل مستخدم صفًّا لكل مهارة لم يلمسها قط، ولصار السؤال «متى يُنشأ؟» بلا إجابة.
**الحل:** `gap` **رتبة عرض** تُحسب من الفرق بين متطلبات الدور والادعاءات، وقيد `claim_gap_is_not_stored` يرفض تخزينها. **التصميم لم يتغيّر — الواجهة تعرض `gap` كما هي.**

---

# أخطاء وقعتُ فيها أثناء هذه الشريحة

> أذكرها لأن تقرير جاهزية يخفي أخطاءه لا يستحق الثقة.

| # | الخطأ | كيف ظهر | التصحيح |
|---|---|---|---|
| 1 | **اختبار يثبت لا شيء** | ثلاثة اختبارات e2e سقطت بـ`25P02`. السبب: بيان مرفوض **يُجهض المعاملة كلها**، وإطلاق الاتصال وهو مُجهض **يسمّم تجمّع الاتصالات** للاختبار التالي | `expectRejected` بـ`savepoint` يعزل كل رفض · و`asAuthenticatedUser` يضمن `rollback` دائمًا |
| 2 | **مسار عام ابتلعته بادئة النسخة** | `/public/reports/:id` أعاد `404` لأن `setGlobalPrefix('v1')` أضاف البادئة إليه | استُثني من البادئة: **رابط المشاركة قد يعيش ٩٠ يومًا ولا يجوز أن ينكسر عند رفع نسخة الـAPI** |
| 3 | **عدّ كاذب في تحققي** | «إثباتات القاعدة: صفر» بينما كانت تمر — `psql -q` **يكتم رسائل NOTICE** التي كنت أعدّها | أُعيد العدّ بلا `-q` ⇒ ٢٩ و٤٢ |
| 4 | **سكربت غير متكرّر يبدو انحدارًا** | «anon sees no unpublished profile» سقط في التشغيل الثاني | السكربت **يزرع بياناته**؛ `db-test.sh` يُنشئ قاعدة جديدة كل مرة. **وُثِّق في رأس الملف** |

**ولا خطأ منها كان في المنتج.** ثلاثة في أدوات اختباري وواحد في تحققي.

---

# المخرجات

| المخرَج | الموقع |
|---|---|
| الكود | `packages/domain` (+٤ وحدات) · `apps/api/src/{auth,infra,slice1}` · `apps/app/src` |
| الهجرات | `supabase/migrations/0003_slice_1.sql` — **٦ جداول و٨ سياسات جديدة** |
| البذرة | `supabase/seed/0001_demo_role.sql` — دور واحد، **موسوم تجريبيًا** |
| عقود الـAPI | `packages/contracts` + **١٩ نقطة نهاية منفَّذة** |
| الشاشات | `/login` `/goal` `/project` `/evaluation` `/asset` `/report` |
| الاختبارات | `packages/domain/src/*.test.ts` · `apps/api/test/slice1.e2e.ts` · `supabase/tests/*.sql` |

---

# Definition of Done — البنود الأربعة عشر

| # | البند | الحالة |
|---|---|---|
| 1 | تشغيل اعتماديات Postgres/Supabase | ✅ Postgres · ⚠️ Supabase بلا Docker |
| 2 | تشغيل Next.js | ✅ |
| 3 | تشغيل NestJS | ✅ |
| 4 | مصادقة مستخدم | ✅ الحارس مُختبَر · ⚠️ لا مشروع Supabase حيّ |
| 5 | إنشاء سياق الهدف | ✅ |
| 6 | إنشاء مشروع | ✅ |
| 7 | تسليم دليل | ✅ |
| 8 | تقييمه | ✅ |
| 9 | نقل الدليل المؤهَّل شرعيًا | ✅ `Practiced → Demonstrated` |
| 10 | بند سيرة مدعوم بدليل | ✅ |
| 11 | اشتراط الاعتماد الصريح | ✅ |
| 12 | إنتاج تقرير الأدلة | ✅ |
| 13 | فرض الملكية والعزل | ✅ |
| 14 | اجتياز اختبارات النطاق والقاعدة وRLS والـAPI والطرف-إلى-طرف | ✅ **٢٠١ فحصًا، صفر فشل** |

**١٢ مكتملة بالكامل · ٢ مكتملتان في الكود ومحدودتا التحقق ببيئة البناء** (١ و٤): لا خدمة Docker، فلم تُشغَّل حزمة Supabase حيّة. الحارس المُختبَر هو الحارس الإنتاجي، والرموز مولّدة بنفس الخوارزمية والمطالبات.

---

# ينتظر قرارك

| # | البند | اقتراحي |
|---|---|---|
| **1** | **التعارض ٧** — «تطبيق» مقابل «معاينة ← اعتماد» | الإبقاء على المعاينة: §9 صريح، والتراجع سطران |
| **2** | **التعارض ١** — نسبة التقدّم في شاشة المهمة | عدّاد مخرجات حقيقي («مخرج ١ من ٣») بدل نسبة مئوية |
| **3** | **رفع الملفات** | الشريحة التالية — يفتح Storage وفحص البرمجيات الخبيثة والروابط الموقّعة |
| **4** | **التحقق على Supabase حيّ** | قبل أي إطلاق. يحتاج بيئة فيها Docker أو مشروع Supabase فعلي |
| **5** | **مسار `Verified`** | بعد بوابة SME. القواعد جاهزة ومُختبَرة؛ ينقص المسار البشري |

**توقفت هنا. لم أتجاوز نطاق الشريحة الأولى.**
