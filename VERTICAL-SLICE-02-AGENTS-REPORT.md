# نَقْلة / NAQLA — Vertical Slice 02 · Agent Logic & Orchestration Report
**التاريخ:** 2026-09-26 · **الحالة:** بانتظار مراجعة المالكة · **المزوّد:** `local-test` — **TEST / NON-PRODUCTION**

> **ما تثبته هذه الشريحة:** حوكمة الوكلاء وتوجيههم — أن الوكيل يقرأ ويحلّل ويقترح ويشرح **ولا يكتب**، وأن النطاق يبقى المرجع.
> **ما لا تثبته:** جودة أي نموذج. لا استدعاء لأي نموذج خارجي؛ `OPEN-023` ما زال يحجبه. **وتحقق Supabase الحيّ ما زال محجوبًا ولا يُدَّعى جاهزية إنتاجية لسلوكه.**

# PROVEN

**التدفق A — التوظيف** (`evidence.demonstrated` → المُنسّق `R1` → وكيل التوظيف → اقتراح `cv_bullet` → تحقق → معاينة → اعتماد → أصل فعّال): يعمل طرفًا إلى طرف على قاعدة PostgreSQL حقيقية. **لا أصل قبل الاعتماد** (صفر صفوف)، والاعتماد ينشئ أصلًا `active` بمصدر `ai_generated` و`drafting_aid_used = true` و`provenance_source = agent_proposal:<id>`، يظهر في تقرير الأدلة.

**التدفق B — التقني** (`evaluation.completed` دون العتبة → `R2` → الوكيل التقني → `rubric_explanation` + `technical_feedback` + `technical_next_action` بحالة `validated`): يعمل، **و`EvaluationResult` وحالة الادعاء وعدد الأدلة بعده كما قبله بالضبط** (مقارنة صفوف).

**الاختبارات السبعة عشر المطلوبة:**

| # | المطلوب | أين أُثبت |
|---|---|---|
| 1 | وكيل التوظيف لا يُنشئ محتوى سيرة نهائيًا | وحدة: مخرجه الوحيد `AgentProposal` بلا فعل · e2e: صفر أصول بعد التوليد |
| 2 | الاقتراح يتطلب اعتمادًا صريحًا | وحدة: `requiresUserApproval` يُفرض `true` للصياغات · e2e: `approved:false` مرفوض |
| 3 | مهارة غير مدعومة تُرفض | وحدة: `self_reported` في `namedSkillIds` ⇒ `ProposalRejected` |
| 4 | مقياس مخترع يُرفض | وحدة (نسبة مئوية) · e2e: عبث بالحمولة المخزَّنة ⇒ الاعتماد `400` |
| 5 | استنتاج إطار يُرفض | وحدة: `React` بالاسم أو في النص بلا تصريح ⇒ مرفوض؛ مصرَّح به ⇒ مقبول · e2e: تعديل المستخدم بـ`React` ⇒ `400` |
| 6 | التقني لا يعدّل `EvaluationResult` | وحدة: مفاتيح فعل ⇒ `forbidden_action` · e2e: الصف مطابق قبل/بعد |
| 7 | التقني لا ينقل الدليل | وحدة: `validation_activity` توصية فقط، `verified` مرفوض · e2e: الادعاء يبقى `practiced` |
| 8 | مخرج الوكيل يُتحقَّق من مخططه | وحدة: كائن فارغ، نص، رقم، `null`، حقل ناقص، حمولة بلا `kind` ⇒ مرفوضة |
| 9 | اقتراح غير صالح يُرفض | وحدة+e2e: مزوّد مشوّه ⇒ صفر اقتراحات، `rejected_invalid_output` |
| 10 | مراجع المصدر محفوظة | e2e: `evidence_refs` = معرّف الدليل؛ `source_refs` تشمل `evaluation_result` |
| 11 | A لا يقرأ اقتراحات B | e2e: `404` قراءةً واعتمادًا ورفضًا · RLS صفر صفوف · تفاصيل الاستدعاء غير مقروءة للعميل |
| 12 | المرفوض يبقى قابلًا للتدقيق | e2e: `rejected` + السبب محفوظ، لا يُحيا، والمعتمد **لا يُعاد كتابته** (مُحفِّز قاعدة) |
| 13 | الاعتماد ينشّط بعد تحقق النطاق فقط | e2e: التحقق يُعاد عند الاعتماد على الوقائع الحالية وعلى تعديل المستخدم |
| 14 | توجيه حدث التوظيف | وحدة+e2e: `R1-demonstrated-to-recruitment` مسجَّل على الاستدعاء |
| 15 | توجيه الحدث التقني | وحدة+e2e: `R2-evaluation-failed-to-technical`؛ اجتياز نظيف ⇒ **لا وكيل تقني** |
| 16 | الرفيق يعرض المُتحقَّق منه فقط | وحدة: `generated`/`rejected` ⇒ لا نكزة · e2e: نكزة واحدة، وتختفي بعد رفض الكل |
| 17 | فشل الوكيل لا يغيّر الحالة المرجعية | وحدة: خطأ مزوّد وميزانية ⇒ سجل لا استثناء · e2e: تطبيق بمزوّد مشوّه — التقييم ينجح، الحالة `demonstrated` كما وضعها التقييم، صفر اقتراحات وأصول |

**والسلبية غير المطلوبة صراحةً:** الوكيلان لا يصدران أنواع بعضهما · الوكلاء الثلاثة الآخرون عقود تمديد ترفضها البوابة · المزوّد الاختباري يُرفض عند `NODE_ENV=production` (البوابة **و**الإعدادات) · تجاوز الميزانية يُسجَّل لا يُرمى · الاستدعاء يحفظ **بالضبط** المسارات الممرَّرة والمحجوبة.

# TEST PROVIDER ONLY

`LocalTestProvider` (`packages/agents/src/local-test-provider.ts`) — يُعلَّم `testOnly`، ينسخ وقائع من السياق **المحجوب** إلى أشكال الاقتراح حتميًا، **ولا يضيف واقعة**. يحمل أربعة أوضاع لحقن الأعطال (`normal` · `malformed` · `error` · `invents`) والوضع الأخير مزوّد يبالغ عمدًا لإثبات أن البوابة ترفضه.

ما يثبته: العقود · التوجيه · التخزين · التحقق · مسار الاعتماد · التنسيق.
ما لا يثبته: جودة الصياغة، جدوى الملاحظات التقنية، سلوك نموذج حقيقي تحت نفس الحوكمة. **التكلفة `null`** في كل سجل استخدام — لم تُختلق.

# NOT IMPLEMENTED

| البند | لماذا |
|---|---|
| وكلاء التعلّم والعلامة الشخصية والأعمال | عقود تمديد فقط (`implemented: false`)، بقرارك |
| وكيل الرفيق المستقل · حوارات متعددة الوكلاء · حلقات ذاتية | بقرارك؛ الرفيق طبقة عرض بنصوص قالبية |
| أنواع الاقتراح `cv_rewrite` · `professional_summary` · `linkedin_*` · `profile_gap` · `improvement_action` · `followup_question` · `validation_activity` · `missing_evidence` | **معرَّفة في التصنيف ومُتحقَّق من مخططها**، لكن المزوّد الاختباري لا يولّدها والتدفقان لا يستعملانها. لا تدفق ثالث |
| قاعدة `R4` (ادعاء غير مدعوم) و`R5` (معيار فاشل) | معرَّفتان ومُختبَرتان وحدةً؛ لا حدث في المنتج يطلقهما بعد |
| `superseded` في الواجهة | الحالة والقيد موجودان؛ لا مسار توليد نسخة جديدة بعد |
| تعديل المستخدم بالإنجليزية | التعديل يمس الصياغة العربية؛ الإنجليزية تُسقط عند التعديل |
| النشر المباشر · `Verified` · تحقق Supabase الحيّ · ميزات جديدة | بقرارك |

# BLOCKED

| # | الحاجب | يمنع |
|---|---|---|
| 1 | **`OPEN-023`** شروط معالجة البيانات لدى مزوّد النماذج | أي مزوّد حقيقي. `AGENT_PROVIDER` يقبل `local-test` فقط، و`verify-boundaries` يرفض أي SDK نماذج |
| 2 | **تحقق Supabase الحيّ** (لا مشروع، لا شبكة) | ادعاء أي جاهزية إنتاجية لسلوك Supabase — بما فيه RLS على `agent_proposal` حيًّا |
| 3 | `OPEN-022` موطن البيانات | الإطلاق |
| 4 | اعتماد SME | `Verified` — والوكيل لا يوصي به أصلًا (D-059) |

# SECURITY / PRIVACY

- **الحد الأدنى من السياق**: قائمة سماح لكل وكيل + قائمة `NEVER` (بريد، مسارات، رموز، محادثات، ملاحظات خاصة) لا تخترقها قائمة سماح. مُختبَر: التوظيف لا يرى `privateNotes` ولا أوصاف المشاريع (يرى `projects.title/kind` فقط)؛ التقني لا يرى `cv` ولا `linkedin`.
- **ما مُرِّر يُسجَّل**: `agent_invocation.allowed_context` و`redacted_context` لكل استدعاء — واقعة لا افتراض.
- **RLS**: المستخدم يقرأ اقتراحاته فقط ولا يكتبها (لا سياسة إدراج/تعديل)؛ `agent_invocation` و`agent_usage` غير مقروءة للعميل إطلاقًا. `UPDATE` غير المسموح يُصفّى إلى صفر صفوف (مُختبَر بشرطَي «صفر صفوف» و«لم يتغيّر»).
- **لا مفتاح خدمة في العميل**: البوابة تعيش في الـAPI فقط؛ الواجهة تتحدث عبر `/v1/me/proposals` بالرمز.
- **تجميد التاريخ**: مُحفِّز `agent_proposal_freeze` يرفض تغيير جوهر المعتمد/المرفوض حتى بصلاحية الخدمة (مُختبَر).
- **لا SDK نماذج في المستودع**: `grep` = صفر، والحارس آلي.

# AGENT GOVERNANCE

الطبقات الست التي تفصل الاقتراح عن الحقيقة، وكلٌّ منها كافية وحدها:

1. **لا مسار كتابة** — `@naqla/agents` يعتمد على `@naqla/domain` فقط؛ لا قاعدة بيانات، لا HTTP.
2. **الحمولة مُنمَّطة** — `structuredPayload` واحد من ثمانية أشكال؛ الكائن الحر ليس اقتراحًا.
3. **مفاتيح الفعل محظورة** — `newState` · `promote` · `evaluationResultPatch` · `outcome` · `score` · `publish` … ⇒ `forbidden_action`.
4. **التحقق بقواعد النطاق مرتان** — عند التوليد وعند الاعتماد، وعلى تعديل المستخدم.
5. **الاعتماد صريح ومسار واحد** — `awaiting_user → approved` بيد المستخدم، والأصل الناتج يخضع لنفس قيود `professional_asset`.
6. **المُنسّق قواعد** — خمس قواعد مُسمّاة، الأولى تفوز، تُسجَّل على الاستدعاء.

**دورة الحياة:** `generated → validated → awaiting_user → approved | rejected | superseded` — المعتمد والمرفوض نهائيان ومجمّدان.

# TEST RESULTS

| الطبقة | العدد | النتيجة |
|---|---|---|
| النطاق | ٩٩ | ✅ |
| الإعدادات | ٨ | ✅ (+`local-test` يُرفض في الإنتاج) |
| **الوكلاء (وحدة)** | **٢٦** | ✅ |
| ثوابت القاعدة | ٢٩ | ✅ |
| وصول RLS | ٤٢ | ✅ |
| طرف-إلى-طرف | **٥١** (٢٩ + ١٢ + **١٠ وكلاء**) | ✅ |
| **المجموع** | **٢٥٥ فحصًا، صفر فشل** | |

بناء نظيف: `@naqla/agents` · `@naqla/api` · `@naqla/app` (+`/proposals`). `verify:boundaries` · `verify:prototype` · `verify:tokens` كلها خضراء. **المجموعات السابقة أُعيدت كاملة** بعد ربط التنسيق بكل تقييم — ٤١/٤١.

**الأرقام:** ١ حزمة جديدة · ٣ جداول · ٥ قيود + مُحفِّز · ٥ نقاط نهاية · ١٧ نوع اقتراح · ٥ قواعد تنسيق · وكيلان منفَّذان من خمسة مسجَّلين · `DOMAIN_RULESET_VERSION` بلا تغيير (**لم تُمس دلالات الدليل**).

# OPEN DECISIONS

| # | البند | اقتراحي |
|---|---|---|
| 1 | **الميزانية**: سقف ٥٠ استدعاء/مستخدم/يوم قيمة مبدئية | تبقى إعدادًا؛ تُعاير بعد مزوّد حقيقي |
| 2 | **التوليد المزدوج لبند السيرة**: المسار الحتمي (`/evidence/:id/cv-bullet`) واقتراح الوكيل كلاهما قائم | إبقاء الاثنين حتى وجود مزوّد حقيقي، ثم قرار |
| 3 | **`R3` (اجتياز بمعيار اختياري ناقص)** يستدعي التقني | مفيد؛ يُعطَّل إن أُريد الهدوء |
| 4 | **قائمة الأطر المرفوضة بالنص** (`React` · `Vue` · …) ثابتة | تُنقل إلى بيانات محتوى مع حزمة المسار |
| 5 | **حذف الأصل بعد اعتماد اقتراح مرفوض دليله** | خارج النطاق؛ يحتاج قرار سحب |

**توقفت هنا. لا وكيل ثالث. بانتظار مراجعتك.**
