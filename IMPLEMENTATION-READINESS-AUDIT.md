# نَقْلة / NAQLA — Implementation Readiness Audit
**الإصدار:** 1.0 · **التاريخ:** 2026-09-26 · **الحالة:** تقرير مراجعة — **لا يغيّر أي متطلب ولا يفتح الأساس الوظيفي**

---

## ٠. ما هذه الوثيقة وما ليست

**هي:** جرد صادق لما أثبته النموذج الثابت فعلًا، وما لم يُبنَ بعد، والقرارات المعمارية التي لم تُتخذ — بصيغة تسمح بتقدير حجم العمل المتبقي قبل الالتزام بأي Stack.

**ليست:** تصميمًا تقنيًا، ولا مخطط قاعدة بيانات، ولا اختيارًا نهائيًا لمزوّد. توصيات القسم «ج» **اقتراحات للاعتماد**، وكل واحدة منها تصبح قرارًا فقط حين تُسجَّل في `docs/decisions/DECISIONS.md`.

### المصادر
| المصدر | ما أُخذ منه |
|---|---|
| `docs/design/NAQLA-DESIGN-HANDOFF-FOR-CODE.md` (v1.0، مُجمَّدة) | ١٧ قسمًا: الرموز، الشبكة، RTL/LTR، المكوّنات، دلالات الدليل والدرجات، الحالات، الوصولية |
| `docs/design/NAQLA-APP.dc.html` | ١٣ شاشة مُجمَّدة — مصدر التخطيط والنسخ |
| `apps/web/` | التنفيذ الثابت: **١٢ صفحة** · **٩٧٦ سطرًا** من CSS/JS · **١٠٠ فئة مكوّن** |
| `docs/srs/SRS-001-career-os-functional.md` v1.3 | **١٠٤ متطلبات وظيفية** · ١٨ قاعدة عمل · الثوابت التسعة |
| `docs/blueprint/19-phase1-feature-inventory.md` | **١٠٨ بنود** في الجرد |
| `docs/blueprint/24-open-decisions-register.md` | **٣٧ بندًا مفتوحًا**، منها **١١ حاجبًا**، و`OPEN-019` مُغلق بـ`CHG-005` |
| `docs/decisions/DECISIONS.md` | D-001 … D-044b |
| نتائج التحقق المُنفَّذة | تصيير آلي في Chromium عند ١٣٦٦ · ٩٠٠ · ٣٩٠ بكسل |

### قاعدة الأرقام
كل عدد في هذه الوثيقة **محسوب آليًا من الملفات نفسها** (D-043b)، لا مُقدَّرًا. وحيثما لم أستطع الحساب كتبتُ ذلك صراحةً.

---

# أ. ما أثبته النموذج الثابت فعلًا

> **حدّ الإثبات:** النموذج يثبت أن التصميم **قابل للتنفيذ كما هو** — تخطيطًا وسلوكًا ولغةً. **ولا يثبت أي شيء عن صحة المنتج** (هل الدليل مقنع؟ هل التقييم عادل؟ هل المستخدم يعود؟) — تلك مهمة Phase 0، ولم تبدأ.

## أ.١ تغطية الشاشات

| بند جرد الصفحات (§2) | الحالة | الملف |
|---|---|---|
| 1 · الرئيسية | ✅ منفَّذة (عربي + إنجليزي) | `index.html` · `en/index.html` |
| 2 · مساري | ✅ | `career.html` |
| 3 · المشاريع | ✅ | `projects.html` |
| 4 · المهمة / مساحة عمل النشاط | ✅ | `mission.html` |
| 5 · المهارات والأدلة + لوحة المهارة المحددة | ✅ | `skills.html` |
| 6 · الملف المهني · نظرة عامة | ✅ | `profile.html` |
| 7 · الملف المهني · السيرة | ✅ | `profile-cv.html` |
| 8 · الملف المهني · لينكدإن | ✅ | `profile-linkedin.html` |
| 9 · الملف المهني · دراسات الحالة | ✅ | `profile-case-studies.html` |
| 10 · الملف المهني · الأصول | ✅ | `profile-assets.html` |
| 11 · التنبيهات · 12 · حالات الرفيق · 13 · فارغ/تحميل/نجاح/انتباه | ✅ مجموعة | `states.html` |
| 14 · لحظات العلامة (`/auth` · onboarding · settings/about · الملف العام · الأصل المُصدَّر) | ⛔ **لم تُنفَّذ** | — |

**المحصّلة: ١٣ من ١٤ بندًا · البند الوحيد الناقص هو ١٤**، وسببه أنه يحتاج تدفّقات (مصادقة، onboarding بسبع خطوات) لا شاشات، وهي خارج الشاشات الـ١٣ المُجمَّدة.

**ما لم يُصمَّم أصلًا فلم يُنفَّذ:** التبويبات الإنجليزية غير الرئيسية — و§17 من وثيقة التسليم يدرجها بنفسه بندًا مفتوحًا **غير حاجب للتجميد**، لأنها تتبع قواعد §5 المنعكسة بلا قرارات تصميم جديدة.

## أ.٢ السلوك المتجاوب

**مُختبَر آليًا عند ثلاثة عروض**، وهي نقاط الانكسار الثلاث في §4:

| العرض | ما يُتحقَّق منه | النتيجة |
|---|---|---|
| ١٣٦٦ بكسل (ديسكتوب ≥1200) | شريط جانبي ٨٨ بكسل · شبكات عمودين · لوحة المهارة ٤٠٠ بكسل · المهمة ١.٥fr/٣٨٠ | ✅ بلا تجاوز أفقي |
| ٩٠٠ بكسل (لوحي 768–1199) | بقاء الشريط الجانبي · انهيار الشبكات لعمود · تمرير التبويبات أفقيًا | ✅ بلا تجاوز أفقي |
| ٣٩٠ بكسل (جوال <768) | شريط سفلي ٧٢ بكسل + `env(safe-area-inset-bottom)` · عمود واحد · حشو سفلي للمحتوى | ✅ بلا تجاوز أفقي |

**يُحسب مُثبتًا:** أن التخطيط المُجمَّد ينهار بلا كسر عند نقاط الانكسار الثلاث في **كل** الصفحات الاثنتي عشرة، لا في عيّنة منها.
**لا يُحسب مُثبتًا:** السلوك على أجهزة حقيقية، ولا `env(safe-area-inset-*)` الفعلي (لا يُحاكى في المتصفح المكتبي)، ولا ما يحدث عند فتح لوحة المفاتيح على iOS — وهو شرط صريح في §11 («يُخفى الرفيق أثناء فتح لوحة المفاتيح»).

## أ.٣ سلوك RTL/LTR

| القاعدة (§5) | كيف نُفِّذت | كيف تحقّقت |
|---|---|---|
| الاتجاه من `dir` على `<html>` | `dir="rtl"` عربي · `dir="ltr"` إنجليزي · القشرة تقرأ `lang` وتبدّل التسميات | ✅ صفحة إنجليزية تعمل بنفس الكود |
| مسافات بخصائص منطقية | `margin-inline` · `inset-inline-start` · `border-inline-end` — **لا `left/right` في أي ملف** | ✅ فحص نصي |
| انعكاس اتجاه التقدّم | تعبئة الشريط مثبّتة من **اليمين** في RTL | ✅ **قياس آلي**: `rightGap = 0` لكل أشرطة الدرجات الثلاثة |
| المصطلح الإنجليزي في مجرى معزول | `.term { direction: ltr; unicode-bidi: isolate }` + `lang="en"` | ✅ |
| السهم لا ينعكس | `.transition` مجرى LTR معزول | ✅ `No evidence → … → Verified` يُعرض بترتيبه |
| الأرقام: غربية في مكوّنات التقدّم · هندية داخل الجمل العربية | `.num` للأولى · نص عادي للثانية (أُزيل `.num` عن ٤٧ عددًا عربيًا) | ✅ فحص آلي لترتيب المحارف |

**ملاحظة صدق:** ظننتُ في مرحلة الفحص أن الفاصل `·` ينقلب أمام الأرقام الهندية، فاختبرتُ خمس صياغات بقياس إحداثيات كل محرف — **الترتيب كان صحيحًا في الخمس**، والالتباس بصري بحت (الرقم `٥` شكله دائرة صغيرة قرب النقطة الوسطى). لا خطأ، ولا تغيير لزم.

## أ.٤ التنقّل

| ما نُفِّذ | الحالة |
|---|---|
| خمسة أقسام بترتيب ثابت (§1) | ✅ مصدر واحد في `app.js`، لا نسخة لكل صفحة |
| شريط جانبي ٨٨ بكسل / شريط سفلي ٧٢ بكسل بنفس مصدر العناصر | ✅ |
| `aria-current="page"` على القسم النشط | ✅ من `data-page` |
| شارة نقطة = «يوجد جديد» · شارة رقمية على **ملفي فقط** | ✅ مع نص بديل مقروء للقارئ الصوتي |
| **الرفيق المهني طبقة لا عنصر تنقّل** | ✅ خارج `NAV` تمامًا |
| تركيب القشرة في كل الصفحات | ✅ **فحص آلي**: `.nav-item` موجودة في ١٢ صفحة × ٣ عروض |
| كل الروابط الداخلية تشير إلى ملفات موجودة | ✅ **فحص آلي**: صفر روابط ميتة |

**لم يُثبت:** المسار الكامل بلوحة المفاتيح عبر التنقّل → الشريط → الخطوة التالية → المحتوى → الرفيق (§13). البنية موجودة والترتيب في DOM صحيح، **لكن لم يُختبر بلوحة مفاتيح فعلية ولا بقارئ شاشة**.

## أ.٥ بصريات حالات الدليل

| القاعدة | التنفيذ | الحالة |
|---|---|---|
| خمس حالات بأشكال مميزة | `dot--gap` متقطعة · `dot--self` مفرغة · `dot--practiced` نصف ممتلئة · `dot--demonstrated` ممتلئة · `seal` ختم | ✅ |
| **لا حالة بلون وحده** | كل حالة تحمل **شكلًا ونصًّا** | ✅ فحص بصري لكل صفحة |
| شريط الدرجة ٤ خلايا (٠–٤) · الخلية الأولى متقطعة عند الفجوة · «قيد الإثبات» مخططة | `.cells` بأربع حالات خلية | ✅ |
| الانتقال **للأمام فقط** `0→4` | لا يوجد في الكود أي تمثيل لانتقال عكسي — **لا فئة، ولا نص، ولا حالة** | ✅ |
| `Practiced` لا تُعرض كادعاء | «ظهرت في مشروع» · «قيد التحقق» · «كمشروع لا كمهارة» في السيرة ولينكدإن | ✅ |
| ادعاء غير مدعوم = حالة انتباه + ملاحظة مسؤول توظيف | `attention-block` + بند مراجعة مخصص | ✅ |
| «فجوة» محجوزة لحالة `gap` وحدها | عنوان «أهم ثلاث نقاط تحتاج تقدّمًا» لا «فجوات» | ✅ |
| التنويه الثابت مع كل درجة | **فحص آلي**: كل صفحة فيها درجة جاهزية تحمل التنويه | ✅ |

**هذا أهم ما أثبته النموذج**: أن **دلالات الدليل قابلة للعرض بلا مبالغة** — أن الواجهة تستطيع أن تقول «لم تُثبت بعد» بوضوح دون أن تبدو عقابية. كانت هذه أكبر مخاطرة تصميمية في المنتج كله.

## أ.٦ بنية التعريب

| ما أُثبت | كيف |
|---|---|
| القشرة **ثنائية اللغة بنيويًا لا بالنسخ** | الاتجاه والتسميات من `<html lang/dir>`؛ جدول `T = { ar, en }` واحد |
| لا نص عربي مُثبَّت في مكوّن | تسميات التنقّل والتنبيهات المولّدة كلها من جدول اللغة |
| الشاشة الإنجليزية تعمل بنفس CSS وJS | `en/index.html` بلا ورقة أنماط خاصة ولا فرع كود |
| مسارات الأصول قابلة للإزاحة | `data-base` يضبط جذر الروابط للغات في مجلدات فرعية |

**الحد:** هذا **هيكل** تعريب لا **نظام** تعريب. لا ملفات رسائل، ولا تعدّد صيغ الجمع العربي (ستة أشكال)، ولا تنسيق تاريخ/رقم حسب اللغة، ولا تعريب للمحتوى نفسه (أوصاف الأنشطة، الرُبريكات، بنود السيرة) — وهذا بند مستقل في القسم «ب».

## أ.٧ افتراضات التفاعل

سبع سلوكيات مُنفَّذة، كلٌّ منها **افتراض قابل للاختبار** لا حقيقة:

| السلوك | الافتراض الذي يجسّده |
|---|---|
| شريط درجة متحرك ٦٠٠ ملّي + شريحة دلتا تنبثق بعده | أن ربط «عملي تغيّر» بـ«رقمي تغيّر» بصريًا يكفي لفهم السببية |
| «كيف حُسبت؟» يفتح منطقة DOM حقيقية (لا tooltip) مع `aria-expanded` | أن المستخدم **يريد** التفصيل عند الحاجة فقط |
| تنبيه واحد في كل مرة بطابور · ٤ ثوانٍ (٨ مع إجراء) · يبقى عند التمرير أو التركيز | أن التنبيهات المتزاحمة تُفقد المعنى |
| الرفيق: صامت / بصيرة جديدة / نكزة / موسَّع + بصيرة داخل الصفحة | أن المساعد الحاضر دائمًا يجب أن يكون **صامتًا افتراضيًا** |
| الرفيق ينكمش إلى علامته إذا تقاطع مع زر أساسي | أن المساعد لا يجوز أن يقف بين المستخدم وفعله |
| «معاينة → اعتماد» قبل أي صياغة مهنية | أن الموافقة الصريحة شرط ثقة لا خطوة زائدة |
| كتلة داكنة واحدة وزر أساسي واحد لكل شاشة | أن «الخطوة الواحدة الآن» أهم من عرض كل الخيارات |

**لم يُختبر أيٌّ منها مع مستخدم واحد.** النموذج يجعلها قابلة للاختبار، ولا يصادق عليها.

## أ.٨ اتساق التصميم

| ما تحقّقت منه آليًا | النتيجة |
|---|---|
| كل فئة CSS مستخدمة في HTML معرَّفة فعلًا | ✅ **صفر فئات غير معرَّفة** |
| بنية HTML سليمة (وسوم متوازنة) | ✅ في ١٢ صفحة |
| كل مرجع `aria-labelledby` / `aria-controls` / `aria-describedby` يشير إلى `id` موجود | ✅ |
| كل هدف `data-drilldown` / `data-toggle` موجود | ✅ |
| **لا برتقالي ولا كهرماني ولا خردلي ولا تدرّج** على أي سطح | ✅ فحص نصي |
| كلمة «أتقنت» / `mastered` ممنوعة | ✅ صفر ورود |
| كتلة داكنة واحدة كحد أقصى لكل صفحة | ✅ |
| صفر أخطاء JavaScript | ✅ في ١٢ صفحة × ٣ عروض |

**تصحيحان أُجريا أثناء التحقق**، مذكوران هنا لأن إخفاءهما يُفسد قيمة التقرير:
1. لون تعبئة أشرطة التقدّم كان `--text` (أسود) بينما الشاشات المُجمَّدة لا تحوي ولا خلفية سوداء واحدة — صُحِّح إلى `--primary`.
2. مسار التبويبات كان يمتد بعرض الصفحة بينما هو في الشاشتين المرجعيتين بمقاس محتواه — صُحِّح.

**انحراف واحد قائم ومعلن**: زر «تطبيق التحسينين» صار «معاينة التحسينين» + خطوة اعتماد، لأن §9 يمنع تسمية الأزرار «تطبيق» ويمنع تطبيق صياغة مولّدة بلا اعتماد صريح — وهذا يعارض نسخة الشاشة. **رجّحتُ القاعدة، والقرار النهائي لك.**

---

# ب. ما ينقص للإنتاج

> ترتيب هذا القسم **ليس ترتيب البناء** — ترتيب البناء في آخر الوثيقة. الرمز: 🔴 يمنع الإطلاق · 🟡 يمنع اكتمال Phase 1 · 🟢 يمكن تأجيله بافتراض معلن.

| # | الناقص | الحالة الآن | ما يلزمه فعلًا | الرابط بالسجلات |
|---|---|---|---|---|
| 1 | **المصادقة** 🔴 | صفر — الصفحات ساكنة بلا جلسة | تسجيل/دخول · جلسة · تدوير · استعادة · تأكيد بريد · تسجيل خروج من كل الأجهزة · حماية من التخمين | البند ١٤ من جرد الصفحات · `OPEN-017` (تبريد `username`) |
| 2 | **تدفّق onboarding** 🔴 | صفر — لا شاشة واحدة من السبع | ٧ خطوات: اللغة → الدور → **تأكيد الهدف** → رفع سيرة أو البدء بدونها → أول مشروع → تقييم أولي → أول خطوة · **مع حالة «البيانات غير مكتملة بعد» بدل اختراع متطلبات** | §1 من وثيقة التسليم · `FR-U-*` |
| 3 | **حسابات المستخدمين** 🔴 | `data.js` يحوي مستخدمًا وهميًا واحدًا | ملف المستخدم · `username` بذرة · `org_id` محفوظ من Phase 1 · إعدادات · تغيير الهدف · حذف الحساب وتصديره (PDPL) | D-005 (username) · D-032 (`org_id`) · D-025 |
| 4 | **مخطط قاعدة البيانات** 🔴 | صفر — النموذج المفاهيمي موجود، لا جداول | تحويل `docs/model/09` إلى مخطط فعلي، **مع فرض الثوابت في القاعدة لا في التطبيق**: INV-1 (لا Claim بلا Evidence) مفتاح أجنبي لا شرط برمجي | `docs/model/09` · الثوابت التسعة |
| 5 | **بقاء المشاريع** 🔴 | ثلاثة مشاريع في `data.js` | مشروع · `WorkItem` · مخرجات · حالات · استئناف بعد توقف · ربط بالمهارات | `FR-U-*` · `docs/blueprint/21` |
| 6 | **بقاء الأدلة** 🔴 | حالات الدليل مرسومة، لا مُخزَّنة | `Evidence` بمصدره وقوته وثقته ونسخة الرُبريك · **سجل انتقالات غير قابل للتعديل** · سحب الدليل يسحب ما اشتُق منه | `docs/model/10` · INV-1 · INV-6 · `FR-P-036` |
| 7 | **محرّك التقييم** 🔴 | «تسليم للتقييم» يُظهر تنبيهًا ويتوقف | استقبال التسليم → فحوص حتمية → تطبيق رُبريك مُصدَّر → فحوص نزاهة → طابور مراجعة بشرية → نتيجة | `docs/evaluation/11` · `12` · `13` |
| 8 | **منطق الدرجات** 🔴 | ٧١٪ و٧٤٪ و٦٨٪ بيانات عيّنة معلنة كذلك | رُبريك جاهزية داخلي لكل من: الملف · السيرة · لينكدإن · اشتقاق الدلتا · تخزين «قبل/بعد» كنسخة · **الدرجة تتغيّر بالعمل لا بتعبئة الحقول** | §9 من وثيقة التسليم |
| 9 | **بيانات إفصاح AI** 🟡 | «سياسة AI» تُعرض كشريحة نصية | سجل إفصاح لكل تسليم · ثلاثة أوضاع (`ai_prohibited` / `ai_assisted` / `ai_expected`) · ست إشارات تثليث · سلّم N0–N4 غير عقابي · **الإفصاح لا يخفض الدرجة بذاته** | `docs/standards/08` |
| 10 | **بقاء تقرير مسؤول التوظيف** 🟡 | أربع ملاحظات ثابتة في HTML | توليد التقرير من بيانات المستخدم · **كل توصية تشير إلى حقل أو دليل فعلي** · نسخ التقرير عبر الزمن · مقارنة قبل/بعد | §9 · `FR-U-*` |
| 11 | **نشر الملف العام** 🟡 | الأصول تقول «لاحقًا — يتغذّى من هنا» | صفحة عامة · رابط · فهرسة أو منعها · انتهاء صلاحية · إلغاء النشر · ماذا يظهر لمن | **`OPEN-016` مفتوح** |
| 12 | **ضوابط المشاركة والخصوصية** 🟡 | صفر | من يرى ماذا · مشاركة دراسة حالة · إخفاء مشروع · سحب موافقة · تصدير البيانات · الحذف | `docs/model/16` · D-025 |
| 13 | **بقاء التنبيهات** 🟡 | التنبيهات تعيش في الذاكرة وتختفي بالتحديث | مركز تنبيهات · مقروء/غير مقروء · «تجاهل هذا الحدث» يُحفظ لكل حدث (§10) · تفضيلات القنوات | `docs/blueprint/22` |
| 14 | **رفع الملفات وتخزينها** 🔴 | منطقة سحب وإفلات بلا وظيفة | رفع · فحص نوع وحجم · فحص برمجيات خبيثة · تخزين خاص افتراضيًا · **عدم قابلية التعديل بعد التقييم** · روابط موقّعة · دورة حياة مرتبطة بسياسة الاحتفاظ | D-024 |
| 15 | **توليد PDF/DOCX** 🟡 | صفر — السيرة تُعرض كتوصيات لا كمستند | بناء سيرة قابلة للتصدير · **جاهزية ATS** (عناوين قياسية، بلا جداول) · نسخ عربية وإنجليزية · حفظ النسخ | §9 · `FR-U-*` |
| 16 | **بقاء المحتوى ثنائي اللغة** 🟡 | هيكل تعريب فقط (أ.٦) | ملفات رسائل · **تعدّد صيغ الجمع العربي** · تنسيق التاريخ والرقم · **وترجمة المحتوى نفسه**: أوصاف الأنشطة والرُبريكات وبنود السيرة ودراسات الحالة | §5 · `docs/data-foundation/29` |
| 17 | **التحليلات وسجل التدقيق** 🟡 | صفر | **فصل صريح**: أحداث التدقيق (بيانات منتج، تخضع للاحتفاظ والامتثال) عن القياس التشغيلي (بيانات تشغيل). INV-7: كل فعل ذي معنى يُصدر حدثًا | INV-7 · D-024 · `docs/blueprint/22` |

### ثلاثة أمور غير تقنية تحجب الإطلاق أيضًا
هذه ليست كودًا، وإغفالها في تقرير جاهزية يجعله مضلِّلًا:

1. **توثيق Data Residency قبل اختيار المزوّد** — `OPEN-022` حاجب **بقرار سابق** (D-004): التوثيق **قبل** الاختيار لا بعده.
2. **مراجعة شروط معالجة البيانات لدى مزوّد النماذج** — `OPEN-023` حاجب؛ قد لا تتطابق مناطقه مع منطقة التخزين.
3. **اعتماد SME** — لا نشاط يمنح `Verified` قبل اعتماد خبير المجال (D-030). حزمة Frontend الحالية كلها `draft` و`can_yield_verified = false`، **وهذا صحيح ومقصود**.

### إحصاء صادق
من **١٠٨ بنود** في جرد المزايا، ما يلمسه النموذج الثابت هو **طبقة العرض فقط**. لم أحسب نسبة «اكتمال» لأن أي نسبة هنا ستكون تقديرًا، وقاعدة D-043b تمنع تقديم التقدير في صورة عدّ. الوصف الدقيق: **الواجهة مُثبتة، والمنتج لم يُبنَ.**

---

# ج. القرارات المعمارية المطلوبة

> كل قرار بالصيغة نفسها: **الموصى به · بديل واحد · لماذا · المقايضة الإنتاجية · هل يمس نطاق MVP**
> السياق الحاكم: **مطوّرة واحدة** (R-1، D-038) · عربي RTL أولًا · PDPL وموطن بيانات · ١٩ وكيلًا خلف بوابة واحدة · تقييم غير متزامن بمراجعة بشرية.

## ج.١ إطار الواجهة الأمامية

| | |
|---|---|
| **الموصى به** | **Next.js (App Router) + TypeScript** |
| **بديل** | SvelteKit |
| **لماذا** | الملف العام لدراسة الحالة يحتاج تصييرًا من الخادم (مشاركة وفهرسة وبطاقات معاينة) — وهذا وحده يرجّح إطارًا يجيد SSR. ولغة واحدة عبر الواجهة والخادم توفّر على مطوّرة واحدة تبديل السياق. والأهم: **الصفحات الاثنتا عشرة تنتقل ١:١** — كل مكوّن فئة CSS ومقطع HTML، بلا مكتبة مكوّنات تُعاد كتابتها. |
| **المقايضة الإنتاجية** | زمن تشغيل أثقل، وجاذبية نحو مزوّد استضافة بعينه. **التخفيف:** إبقاءه قابلًا للنشر على أي مضيف Node بحاوية، وعدم استعمال أي ميزة خاصة بمنصة واحدة. |
| **نطاق MVP** | **لا يمسّه.** الشاشات نفسها، والنقل ميكانيكي. |
| **لماذا لا البديل** | SvelteKit أخف وأسرع فعلًا، لكن نظامه البيئي أصغر، والفارق لا يُبرّر المخاطرة حين يكون الفريق شخصًا واحدًا. |

## ج.٢ إطار الخادم

| | |
|---|---|
| **الموصى به** | **نفس تطبيق Next.js للـAPI + عملية عامل منفصلة**، مع **حزمة نطاق (`domain`) لا تستورد أي إطار** |
| **بديل** | خدمة مستقلة بـFastAPI (بايثون) |
| **لماذا** | نشرة واحدة ونظام أنواع واحد لشخص واحد. **والشرط غير القابل للتفاوض** هو حزمة النطاق المعزولة: منطق الدليل والتقييم والاشتقاق **هو المنتج**، وإذا تسرّب إلى طبقة الإطار صار تغيير الإطار لاحقًا إعادة بناء للمنتج. |
| **المقايضة الإنتاجية** | الخلط بين الواجهة والنطاق في مستودع واحد يغري بالاختصار. **التخفيف:** قاعدة استيراد مفروضة آليًا — `domain` لا يستورد من `web`، ولا يعرف HTTP. |
| **نطاق MVP** | **لا يمسّه.** |
| **لماذا لا البديل** | FastAPI أفضل **إن** انجرف التقييم نحو أدوات بايثون (NLP/ML). لكنه اليوم لغتان ونشرتان لشخص واحد. وبوابة الذكاء الاصطناعي (ج.٦) تسمح بفصل محرّك التقييم لاحقًا كخدمة بلا إعادة بناء — **فالباب يبقى مفتوحًا بتكلفة صفر**. |

## ج.٣ قاعدة البيانات

| | |
|---|---|
| **الموصى به** | **PostgreSQL مُدار، في منطقة يمكن تسميتها** |
| **بديل** | قاعدة مستندات (MongoDB) |
| **لماذا** | النموذج علائقي وحرج التكامل: `Claim` ↔ `Evidence` ↔ `Activity` ↔ نسخة الرُبريك. **INV-1 «لا Claim بلا Evidence» يجب أن يكون مفتاحًا أجنبيًا لا شرطًا برمجيًا** — لأن الشرط البرمجي يُنسى في أول مسار جانبي. و`JSONB` يستوعب مواصفات الأنشطة والرُبريكات المتغيّرة، وسجل الأحداث جدول لا يقبل إلا الإضافة. |
| **المقايضة الإنتاجية** | انضباط هجرات إلزامي، وتوفّر Postgres مُدار **داخل المنطقة** قد يكون محدودًا — وهذا يربطه مباشرة بـ`OPEN-022` الحاجب. |
| **نطاق MVP** | **لا يمسّه** — هو الافتراض الضمني في `docs/model/09` أصلًا. |
| **لماذا لا البديل** | قاعدة المستندات أسرع في البداية مع مواصفات أنشطة متغيّرة، **لكنها تنقل الثوابت من القاعدة إلى التطبيق** — وهو بالضبط ما تمنعه INV-1 وINV-6. |

## ج.٤ مزوّد المصادقة

| | |
|---|---|
| **الموصى به** | **مكتبة مصادقة تُخزّن الهوية في قاعدتك** (Auth.js + محوّل Postgres) **بدخول بلا كلمة مرور (رمز لمرة واحدة عبر البريد)** |
| **بديل** | مزوّد هوية مُدار (Clerk / Auth0) |
| **لماذا** | D-004 يشترط توثيق موطن البيانات، ومزوّد الهوية الخارجي **ينقل بيانات شخصية خارج المنطقة** قبل أن تُوثَّق أصلًا. والدخول بلا كلمة مرور **يزيل أخطر مسؤولية أمنية** (تخزين كلمات المرور) بدل أن يوكلها. |
| **المقايضة الإنتاجية** | تملكين دورة الجلسة والتدوير والتحقق من المعدل والتحقق بخطوتين لاحقًا — **عمل حقيقي لا يُستهان به**، ويحتاج مراجعة أمنية مستقلة قبل الإطلاق. |
| **نطاق MVP** | **يمسّه.** الخطوة الأولى في onboarding، و`username` كبذرة (D-005)، وتبريده بعد الحذف (`OPEN-017` مفتوح). |
| **لماذا لا البديل** | Clerk أسرع بأسابيع فعلًا. **فإن اخترتِه، تسبقه مراجعة موطن بيانات مكتوبة** — لا بعده، احترامًا لـD-004. |

## ج.٥ تخزين الملفات

| | |
|---|---|
| **الموصى به** | **تخزين كائنات متوافق مع S3 في المنطقة نفسها** · خاص افتراضيًا · روابط موقّعة قصيرة العمر |
| **بديل** | تخزين المزوّد المدمج مع قاعدة البيانات (مثل Supabase Storage) |
| **لماذا** | التسليمات مادة الدليل: يجب أن تكون **خاصة افتراضيًا، وغير قابلة للتعديل بعد التقييم**، ومثبّتة في منطقة معروفة. الرابط الموقّع يمنع الوصول المفتوح، ونسخة ما بعد التقييم تحمي سلسلة الإثبات. |
| **المقايضة الإنتاجية** | مكوّن إضافي بسياسة دورة حياة يجب **ربطها بسياسة الاحتفاظ لكل فئة** (D-024) وبالتعليق القانوني — وهذا يُنسى عادة حتى أول طلب حذف. |
| **نطاق MVP** | **يمسّه.** تسليم المهمة بلا رفع = لا تقييم = لا دليل. |
| **لماذا لا البديل** | التخزين المدمج أبسط وأسرع، لكنه يربط قرار التخزين بقرار قاعدة البيانات، ويجعل الخروج لاحقًا أصعب. |

## ج.٦ تجريد مزوّد الذكاء الاصطناعي

| | |
|---|---|
| **الموصى به** | **بوابة داخلية واحدة (AI Gateway — LBD-06)**: واجهة واحدة · قياس لكل استدعاء · مزوّد واحد خلفها في البداية |
| **بديل** | استدعاء مباشر لحزمة المزوّد من كل وكيل |
| **لماذا** | **INV-5 يشترط قياس كل استدعاء نموذج** — وهذا غير قابل للتحقق مع استدعاءات متناثرة. و`OPEN-023` يحذّر أن مناطق المزوّد قد لا تطابق منطقة التخزين، فالبوابة هي المكان الوحيد الذي يُفرض فيه ذلك. والرفيق المهني (A01) يحتاج **تدرّجًا ثلاثيًا للميزانية** (كامل → منخفض التكلفة → حتمي، **بلا توقّف أبدًا**) وهو مستحيل بلا بوابة. |
| **المقايضة الإنتاجية** | طبقة إضافية وزمن استجابة إضافي. **والخطر الحقيقي**: أن تُصمَّم البوابة على «القاسم المشترك الأدنى» فتُفقد مزايا المزوّد (المخرجات المهيكلة، التخزين المؤقت للسياق). **التخفيف:** تُصمَّم حول **القدرات** لا حول أبسط واجهة. |
| **نطاق MVP** | **يمسّه — وهي شرط لا خيار.** ثابت معتمد، لا قرار مفتوح. |
| **لماذا لا البديل** | الاستدعاء المباشر أسرع لوكيل واحد، ويصبح غير قابل للحوكمة عند الوكيل الخامس — ولدينا ١٩. |

## ج.٧ المهام الخلفية

| | |
|---|---|
| **الموصى به** | **طابور فوق PostgreSQL** (pg-boss / Graphile Worker) |
| **بديل** | Redis + BullMQ |
| **لماذا** | **الإدراج في الطابور يصير جزءًا من المعاملة نفسها** التي أنشأت التسليم — فلا تسليم يضيع بلا تقييم، ولا تقييم يعمل على تسليم غير محفوظ. وهذا بالضبط ما يحمي الوعد المعروض في الواجهة: «النتيجة خلال دقائق». ومخزن واحد أقل بكثير لشخص واحد. |
| **المقايضة الإنتاجية** | سقف إنتاجية أدنى، وأدوات مراقبة أفقر. **الانتقال لاحقًا مقبول** لأن حدود المهام ستكون معرَّفة سلفًا. |
| **نطاق MVP** | **يمسّه.** التقييم غير متزامن بالتصميم، وكذلك إعادة بناء الإسقاطات وتوزيع التنبيهات. |
| **لماذا لا البديل** | Redis أعلى إنتاجية، لكنه **مخزن ثانٍ ووضع فشل ثانٍ**، ويُفقد الإدراج خاصيته المعاملاتية. |

## ج.٨ النشر

| | |
|---|---|
| **الموصى به** | **صورة حاوية واحدة (تطبيق + عامل) على مضيف حاويات مُدار في منطقة مُسمّاة** · بيئات: تطوير · تجريب · إنتاج |
| **بديل** | منصة نشر مُحسَّنة للإطار + مضيف منفصل للعامل |
| **لماذا** | **`OPEN-022` حاجب على الإطلاق، وD-004 يشترط توثيق موطن البيانات قبل الاختيار.** الحاوية تحفظ حرية الاختيار: تبني الآن وتنشر حيث يسمح التوثيق لاحقًا، بلا إعادة بناء. |
| **المقايضة الإنتاجية** | إعداد أولي أبطأ من منصة جاهزة، ومسؤولية تحديثات الصورة وفحص الثغرات عليك. |
| **نطاق MVP** | **لا يمسّ النطاق** — لكنه **يمنع الإطلاق**، لا البناء. يمكن البناء كاملًا قبل حسمه. |
| **لماذا لا البديل** | المنصة الجاهزة أسرع بكثير، لكن **التحكم بالمنطقة والمهام طويلة الأمد أضعف فيها** — وكلاهما شرط هنا لا تفضيل. |

## ج.٩ الرصد والمراقبة

| | |
|---|---|
| **الموصى به** | **فصل صارم بين مخزنين**: (١) **أحداث التدقيق داخل Postgres** بوصفها بيانات منتج · (٢) **القياس التشغيلي خارجها**: سجلات مهيكلة + تتبّع أخطاء + فحص توفّر |
| **بديل** | منصة رصد واحدة تستقبل كل شيء |
| **لماذا** | INV-7 يوجب أن **كل فعل ذي معنى يُصدر حدثًا**، وهذه الأحداث **بيانات منتج**: تخضع لسياسة الاحتفاظ، ولحق التصدير، وللتعليق القانوني، ويُبنى عليها سجل قرار المراجع **غير القابل للتعديل** (`FR-P-037`). خلطها بضجيج التشغيل يعني تطبيق قواعد PDPL على سجلات لا تحتاجها، **أو الأسوأ: تطبيق سياسة حذف تشغيلية على دليل يجب أن يبقى**. |
| **المقايضة الإنتاجية** | مخزنان للاستعلام، وربط يدوي عند تتبّع حادثة عبرهما. **ثمن مقبول** مقابل وضوح الحوكمة. |
| **نطاق MVP** | **يمسّه جزئيًا**: أحداث التدقيق **إلزامية من اليوم الأول** (ثابت معتمد). القياس التشغيلي يبدأ بحدّه الأدنى: أخطاء + توفّر + سجلات مهيكلة. |
| **لماذا لا البديل** | المنصة الواحدة أبسط تشغيليًا، لكنها **تضع دليل المستخدم تحت سياسة احتفاظ أداة تشغيل** — وهذا خطأ حوكمة لا خطأ هندسة. |

### قرار غير مطلوب الآن — ويُسجَّل أنه كذلك
**لا حاجة لقرار «معمارية الوكلاء» منفصل.** خريطة العقود (`docs/agents/05`) تعرّف ١٩ وكيلًا بعقود مكتوبة، وقاعدة **«الوكلاء يقترحون ولا يكتبون» (LBD-04)** تجعلهم دوال خلف البوابة لا خدمات مستقلة. أي إطار تنسيق وكلاء الآن **تعقيد قبل أوانه**.

---

## Production Build Order

> تسلسل عملي من الأساس إلى النشر. **كل مرحلة لها بوابة خروج** — لا يُنتقل قبل تحققها.
> المبدأ الحاكم: **ما يحمل الثوابت يُبنى أولًا**، لأن تصحيحه لاحقًا إعادة بناء لا تعديل.
> التقديرات الزمنية **مقصودة الغياب** — D-037 يمنع اختراع أرقام، وPhase 0 هو من يقيسها.

### المرحلة ٠ · إزالة الحواجب غير التقنية *(بالتوازي مع كل ما بعدها)*
لا كود. **توثيق موطن البيانات (D-004) → ثم** اختيار المزوّد (`OPEN-022`) · مراجعة شروط معالجة البيانات لدى مزوّد النماذج (`OPEN-023`) · تثبيت نطاق السعر للاختبار (`OPEN-004`) · تأكيد المسار الأول (`OPEN-001`).
**بوابة الخروج:** لا قرار بنية تحتية يُتخذ قبل أن يسبقه توثيقه.

### المرحلة ١ · حزمة النطاق قبل أي إطار
النموذج والثوابت كشيفرة خالصة بلا HTTP ولا قاعدة بيانات: سلّم الدليل · الانتقال للأمام فقط `0→4` · `Claim` بلا `Evidence` **مستحيل بنيويًا** · نسخ الرُبريك · فئات المصدر والثقة · قاعدة الدليلين ومسار استثنائها.
**بوابة الخروج:** اختبارات وحدة تُثبت أن **كل ثابت من التسعة يفشل بصوت مسموع** عند خرقه. هذه أرخص نقطة في المشروع كله لاكتشاف خطأ في النموذج.

### المرحلة ٢ · المخطط والهجرات
اشتقاق الجداول من المرحلة ١. **الثوابت في القاعدة**: مفاتيح أجنبية، قيود `CHECK`، جدول أحداث لا يقبل إلا الإضافة، جداول قرار غير قابلة للتعديل. بذور لحزمة بيانات Frontend القائمة.
**بوابة الخروج:** محاولة إدخال `Claim` بلا `Evidence` **ترفضها القاعدة نفسها**، لا التطبيق.

### المرحلة ٣ · الهوية والحساب
المصادقة (ج.٤) · ملف المستخدم · `username` وتبريده · `org_id` محفوظ · حذف الحساب وتصديره منذ اليوم الأول لا لاحقًا.
**بوابة الخروج:** مستخدم حقيقي يسجّل، يدخل، يصدّر بياناته، ويحذف حسابه — كاملًا.

### المرحلة ٤ · نقل الواجهة إلى الإطار
الصفحات الاثنتا عشرة → مكوّنات، **مع بقاء `tokens.css` كما هو**. استبدال `data.js` بطبقة بيانات حقيقية. onboarding السبع خطوات. البند ١٤ من جرد الصفحات (المصادقة · الإعدادات › عن · لحظات العلامة).
**بوابة الخروج:** **إعادة تشغيل نفس فحوص التحقق آليًا** — ثلاث نقاط انكسار · صفر تجاوز أفقي · صفر أخطاء · تثبيت التعبئة من اليمين في RTL · كل فئة معرَّفة. **النموذج الثابت هو اختبار الانحدار البصري.**

### المرحلة ٥ · المشاريع والتسليم والتخزين
المشروع و`WorkItem` والمخرجات وحالاتها · الرفع وفحصه · عدم قابلية التعديل بعد التقييم · الطابور المعاملاتي (ج.٧).
**بوابة الخروج:** تسليم حقيقي يُحفظ ويدخل الطابور ولا يضيع عند إعادة تشغيل العملية.

### المرحلة ٦ · محرّك التقييم والمراجعة البشرية
الفحوص الحتمية → الرُبريك المُصدَّر → فحوص النزاهة → **واجهة المراجعة العمياء** وسجل القرار غير القابل للتعديل (`FR-P-037`) → إنشاء الدليل → اشتقاق `Achievement` حتميًا (`FR-P-036`).
**بوابة الخروج:** تسليم واحد يمرّ المسار كاملًا وينتج دليلًا بمصدره ونسخة رُبريكه — **وسحب الدليل يسحب ما اشتُق منه**.
**تنبيه:** لا نشاط يمنح `Verified` قبل اعتماد SME (D-030). الحزمة الحالية `draft` و`can_yield_verified = false`، ويبقى كذلك حتى الاعتماد.

### المرحلة ٧ · الدرجات والإسقاطات والأصول
رُبريكات الجاهزية الثلاثة · الدلتا و«قبل/بعد» كنسخ · تقرير مسؤول التوظيف من بيانات فعلية · بنود السيرة ومهارات لينكدإن ودراسات الحالة · **دورة معاينة → اعتماد → تطبيق → إعادة تقييم** · توليد PDF/DOCX بجاهزية ATS.
**بوابة الخروج:** **كل رقم معروض قابل للإرجاع إلى حدث في حساب المستخدم** — وهذا اختبار قاعدة §9 المُلزِمة بعينه.

### المرحلة ٨ · التنبيهات والإفصاح والخصوصية
بقاء التنبيهات وتفضيلاتها · سجل إفصاح AI بأوضاعه الثلاثة وسلّمه غير العقابي · ضوابط المشاركة والخصوصية · الملف العام (**بعد إغلاق `OPEN-016`**) · سياسات الاحتفاظ لكل فئة.
**بوابة الخروج:** مستخدم يستطيع أن يرى ما يظهر عنه، ولمن، ويوقفه.

### المرحلة ٩ · التعريب الكامل
ملفات الرسائل · **صيغ الجمع العربي الست** · تنسيق التاريخ والرقم · **تعريب المحتوى نفسه** (أنشطة، رُبريكات، بنود سيرة) · التبويبات الإنجليزية المتبقية وفق §5 المنعكسة.
**بوابة الخروج:** تبديل اللغة لا يكسر تخطيطًا ولا يترك نصًّا غير مترجم.

### المرحلة ١٠ · الرصد والنشر
الفصل بين مخزني التدقيق والقياس (ج.٩) · الحاوية والبيئات الثلاث · النسخ الاحتياطي **وتجربة الاسترجاع فعليًا** · مراجعة أمنية للمصادقة والرفع والروابط الموقّعة · مراجعة خصوصية سابقة للإطلاق (D-024 · D-025).
**بوابة الخروج:** استرجاع ناجح من نسخة احتياطية في بيئة نظيفة. النسخة غير المُجرَّبة ليست نسخة.

### ما يجري بالتوازي طوال الوقت
**Phase 0 اليدوي** (D-026) — يعمل بالتوازي ولا تحجبه الهندسة: إكمال الأنشطة · جودة التقييم · اتفاق AI مع المراجع البشري · **قيمة `Verified` لدى مسؤولي التوظيف** · الاستعداد للدفع · تكلفة إنتاج النشاط وتكلفة AI لكل دليل.

> **الالتزام المعلن يبقى نافذًا:** لا يُسوَّق `Verified` بوصفه معترفًا به من أصحاب العمل قبل وجود دليل ميداني (D-034).

---

## خلاصة في ثلاث جمل

١. **الواجهة مُثبتة**: ١٣ من ١٤ بندًا في جرد الصفحات، متحققة آليًا عند ثلاث نقاط انكسار، بدلالات دليل لا تبالغ — وهي أكبر مخاطرة تصميمية عبرناها.
٢. **المنتج لم يُبنَ**: لا هوية، ولا بقاء، ولا تقييم، ولا درجات — والنموذج لا يدّعي غير ذلك، فكل رقم فيه معلن بوصفه بيانات عيّنة.
٣. **تسعة قرارات معمارية تنتظر اعتمادك**، وثلاثة حواجب منها **غير تقنية** وتسبق أي كود: موطن البيانات، وشروط مزوّد النماذج، واعتماد خبير المجال.

**الحالة: بانتظار الاعتماد. لم يبدأ أي نقل إلى إطار عمل.**
