# نَقْلة / NAQLA — Data Integration Phase Report · Career Data Foundation
**التاريخ:** 2026-09-26 · **الحالة:** بانتظار مراجعة المالكة · **المسار الأول:** Junior Web / Frontend Developer (`trk_frontend_junior@0.1.0`) — **DEMO / DRAFT / NOT SME APPROVED**

> **ما تثبته هذه المرحلة:** أن أساس البيانات المعتمد صار **جداول وقيودًا ومُحفِّزات وخطًّا** في PostgreSQL، وأن المنتج والوكيلين يقرآن بيانات مهنية مُهيكلة بدل افتراضات، وأن **لا شيء يُعتمد أو يُنشر بغير شخص**.
> **ما لا تثبته:** صحة محتوى الحزمة الأولى. كل صف فيها مسودة صاغها مساعد (drafting_aid = ai_assisted) وينتظر مؤلف محتوى ثم SME. **لا إشارة سوق استُوردت. لا وكيل ثالث. لا مزوّد LLM. لا واجهة جديدة. تحقق Supabase الحيّ ما زال محجوبًا.**

**ما لم يُبنَ عمدًا:** مسار IT Support · مسار تحليل الأعمال · وكيل التعلّم · Market Insights · استيراد سوق العمل · وكيل ثالث · مزوّد LLM حقيقي · واجهة لإدارة المحتوى أو لعرض المسار الجديد.

---

# PROVEN

| # | ما هو مُثبَت | أين |
|---|---|---|
| 1 | **سجل مهارات عالمي** بأعمدة المهمة كلها (`skill_id`=uuid · أسماء كانونية · عائلة · نوع · وصف · مؤشرات ملاحَظة · أنماط فشل · `ai_substitutability` · سياسة حداثة · مصادر · حالة مراجعة · إصدار · تواريخ)؛ «أساسية للدور» على **الربط** لا المهارة | `0007` · `FRONTEND-TRACK-PACK.md` · Q15 |
| 2 | **المترادفات** صريحة بستّ علاقات؛ الصيغة والربط لا يختلطان (قيد)؛ **لا دمج آلي**؛ **لا حذف** فلا إعادة استخدام معرّف (مُحفِّز + إثبات قاعدة) | `skill_synonym` · `invariants.sql` CDF |
| 3 | **تعريف الدور** بحقول المهمة، مستقل عن أي إطار (`validatePack` يرفض إطارًا؛ اختبار سلبي) | `target_role` · `role_tool` · e2e |
| 4 | **RoleSkill** (`role_requirement`) بالحقول المطلوبة؛ متطلب الدور ≠ دليل المستخدم (**مُختبَر:** اقتراح فجوة لا يُنشئ دليلًا ولا ادعاءً) | e2e agent integration |
| 5 | **٥ مهارات أساسية** ولكل منها مؤشرات · مهمة · مسار دليل · تغطية نشاطين · تغطية رُبريك · `demonstrated` ممكن · **`verified` غير ممكن** (موثَّق بالناقص) | `coreSkillEvidencePaths` · Q06 |
| 6 | **١١ مهمة** بالحقول المطلوبة وأنماط فشل بلغتين | `task` · `task_skill` |
| 7 | **٣ أنشطة** تقيس ٢–٣ أساسية بعمق، بمدخلات ومخرجات ومهارات ومهام وفحوص نزاهة مُصنَّفة (٣ user-facing + ٥ assessment-only أنواع) و`can_yield_verified = false` | `activity_*` · `integrity_check_spec.check_type` |
| 8 | **٣ رُبريكات مُهيكلة** · ٢٢ بندًا كلها مربوطة بمهارة (NOT NULL) · واصفات لكل مستوى · فحص حتمي حيث أمكن · لا درجة كلية؛ **المُقيِّم يقرأ الصفوف** (الشريحة ١ ما زالت خضراء عليها) | `rubric_criterion` · `evaluation.service` |
| 9 | **موارد التعلّم** بيانات وصفية فقط؛ `url = null` و`unverified` في كلها (قيد يمنع غير ذلك) | Q08 · `learning_resource` |
| 10 | **الإسناد** لكل سجل (`source_ref`)؛ مصدر بلا ترخيص لا يدخل؛ `market_signal` في المخطط **وبلا سجلات** | Q11 · Q13 |
| 11 | **الطبقات والمراجعة**: raw (غير قابل للتعديل) → normalized (مُعاد التوليد) → curated → approved → published → superseded؛ SME مُسمّى إلزامي؛ demo لا يُعتمد أبدًا ولا يُنشر في الإنتاج (**الـAPI يرفض الإقلاع**) | النطاق · مُحفِّزان · `review_log` · e2e |
| 12 | **الخط** Source → Raw → Normalize → Deduplicate → Map → Review → Approve → Publish؛ متكرّر؛ يرفض المرجع غير المحلول قبل أي كتابة؛ **تقرير تكرار** يقترح ولا يدمج | `pipeline.ts` · `data/career/reports/near-duplicates.md` |
| 13 | **١٦ قاعدة جودة** (المهمة §19 كلها + ٥) تفشل بصوت مسموع | `quality-rules.ts` |
| 14 | **الوكيلان يقرآن بيانات مُهيكلة** أو يعلنان النقص | e2e · `local-test-provider` |

# DRAFT DATA
| السجل | العدد | الحالة |
|---|---|---|
| مهارات (جديدة في السجل العالمي) | 12 (+3 من الشريحة ١ = 15) | `draft` · demo |
| مترادفات/روابط | 18 | `draft` · demo |
| عائلات · مقياس (٤ مستويات) · سياسات حداثة | 8 · 1 · 4 | `draft` · demo (النوافذ للمعايرة، D-023) |
| الدور | 1 (`frontend-developer-junior`) | `draft` · demo |
| ربط دور↔مهارة | 13 (٥ أساسية) | `draft` · `weight = NULL` |
| مهام | 11 | `draft` |
| أنشطة | 3 | `draft` · `can_yield_verified = false` |
| رُبريكات / بنود / واصفات | 3 / 22 / 69 | `draft` · **١٩ بندًا بشريًا** |
| فحوص نزاهة | 18 (+2 الشريحة ١) | داخل الأنشطة |
| موارد تعلّم | 10 | `unverified` · بلا روابط |
| مصادر | 7 (+1 الشريحة ١) | `draft`؛ الرسمية بلا لقطة L0 |
| قواعد عرض | 15 (٥ × ٣) | `draft` |
| مكتبة بنود عامة | 10 (استُخدم ٥ منها في ٧ بنود = ٣٢٪) | `draft` |
| لقطات L0 / سجلات L1 / مرشّحات تكرار / سجل مراجعة / إشارات سوق | 15 / 61 / 1 / 0 / **0** | — |
**لا بيانات مستخدم في جداول المهن.** الدور التجريبي لا يُفترض لكل المستخدمين (الهدف المهني اختيار لكل مستخدم).

# SME REVIEW REQUIRED
كل شيء أعلاه. تفصيل الأسئلة في `docs/data/SME-REVIEW-PACK-FRONTEND.md`: الدور · المهارات الأساسية الخمس والتكرار `ui-state-management`/`skl_ui_state_interaction` · المستويات المستهدفة · المهام وأنماط الفشل · واقعية الأنشطة وعدالة الفحوص المزروعة · واصفات البنود والعتبات والأوزان · تسمية عربية سوقية · سياسات الحداثة. **لا شيء وُسم `sme_reviewed` أو `approved`** — `review_log` فارغ.

# NOT IMPLEMENTED
- مسار المراجعة البشرية لتقييم البنود `human`/`llm` — الأنشطة الثلاثة **لا تُقيَّم** حاليًا (المُقيِّم يرفض صراحةً) — `OPEN-041`.
- ترقية حزمة demo إلى محتوى حقيقي (إزالة `is_demo_fixture`) — `OPEN-040`.
- لقطات L0 للمصادر الرسمية وتأكيد التراخيص — `OPEN-042`.
- تقارير الخط الأخرى (Unmapped · Staleness · Review Queue · Bilingual Gaps) — موجود منها Near-Duplicate وقواعد الجودة فقط.
- واجهة لعرض المسار الجديد أو إدارة المراجعة — ممنوعة في هذه المرحلة.
- وكيل التعلّم — لكن السلسلة `learning_gap → learning_resource → practice_activity → activity_spec` جداولٌ جاهزة.

# BLOCKED
- **تحقق Supabase الحيّ** ما زال محجوبًا (بلا تغيير): الهجرة `0007` وسياسات RLS الجديدة مُثبَتة على PostgreSQL محلي فقط.
- **الاعتماد الحقيقي** محجوب حتى يوجد SME (D-030/D-038) — والحزمة demo لا تقبل الاعتماد بنيويًا.
- التقاط المصادر الرسمية محجوب بغياب الشبكة وحسم التراخيص (`OPEN-008`).

# DATA QUALITY
`npm run career:validate` على قاعدة نظيفة بعد الاستيراد: **١٦ PASS · ٠ FAIL · ٢ WARN** (Q07b: نشاط ورُبريك الشريحة ١ التجريبيان منشوران — مسموح خارج الإنتاج؛ Q18: `ui-testing` في ربط الدور الجديد بلا نشاط/مهمة تقيسها).
**ما كشفته القواعد أثناء البناء (أُصلح):** الدور التجريبي عدّ `ui-state-management` أساسية بلا مسار دليل (Q06) · إعادة الاستيراد تركت إسنادًا معلَّقًا (Q11) · `published_at` لم يُضبط على المسار التجريبي.
**تقرير التكرار:** مرشّح واحد `ui-state-management` ↔ `skl_ui_state_interaction` (0.9، عربي) — **مقترح، غير مدمج** (`OPEN-039`).

# AGENT INTEGRATION
| الوكيل | يقرأ | مُختبَر |
|---|---|---|
| التوظيف | متطلبات الدور **المنشورة** + حالات الادعاءات | دور غير منشور ⇒ تحذير «role requirements unpublished» ولا `profile_gap` · دور منشور بمتطلبين أساسيين ⇒ `profile_gap` واحد للمهارة بلا دليل، **صفر أدلة/ادعاءات جديدة** |
| التقني | النشاط المُهيكل + بنود الرُبريك المنشور مع مهاراتها | `missing_evidence` يسمّي `act_fe_003` و`file.component` · كل بند مشروح يحمل `linkedSkillId` |
منصّة التقييم (٣٠ سيناريو) ما زالت **٢٩ PASS · ١ GAP موثَّق** بعد التغيير (`provider 0.3.0` · `schema 1.2.0` · `domain 0.5.0`).

# TEST RESULTS
| المجموعة | العدد | النتيجة |
|---|---|---|
| `@naqla/domain` (منها 13 لدورة المراجعة/التكرار/المسارات) | 117 | PASS |
| `@naqla/config` · `@naqla/agents` (حوكمة + منصّة) | 9 · 77 | PASS |
| `apps/api` e2e — `career-data` (خط · مراجعة · RLS · حارس الإنتاج · تكامل الوكلاء) | 11 | PASS |
| `apps/api` e2e — `slice1` 29 · `hardening` 12 · `agents` 10 · `withdrawal` 2 | 53 | PASS |
| `scripts/db-test.sh` — إثباتات القيود (منها **١٣ لحوكمة بيانات المهن**) وRLS | 84 PASS · 0 FAIL | PASS |
| `career:import` مرتين · `career:validate` | — | متكرّر · PASSED |
| `verify:boundaries` · `verify:prototype` · `verify:tokens` · Next typecheck | — | PASS |

# OPEN DECISIONS
| # | ما ينتظر المالكة |
|---|---|
| `OPEN-039` | التكرار `ui-state-management` / `skl_ui_state_interaction`: دمج · فصل · إعادة تسمية |
| `OPEN-040` | آلية ترقية حزمة DEMO إلى محتوى حقيقي بعد اعتماد SME |
| `OPEN-041` | مسار المراجعة البشرية للبنود `human` قبل تفعيل الأنشطة الثلاثة |
| `OPEN-042` | التقاط المصادر الرسمية وتأكيد تراخيصها |
| `OPEN-043` | أوزان الربط وعتبات الرُبريك — للمعايرة |
| D-084 | تأكيد المهارات الأساسية الخمس (أو استبدال) قبل مراجعة SME |
| — | هل تبقى `ui-testing` في ربط الدور الجديد بلا نشاط يقيسها؟ |
