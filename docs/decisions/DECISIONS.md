# نَقْلة / NAQLA — Decision Log (قرارات معتمدة)
> قرارات مُعتمدة رسميًا من المؤسِّس. تغييرها يتطلب قرارًا جديدًا موثّقًا هنا، لا نقاشًا عابرًا.

| # | القرار | الحالة | الأثر |
|---|---|---|---|
| D-001 | لا يُحذف أي عنصر من الرؤية؛ يُصنَّف ويُرحَّل مع نقطة تمديد واضحة | معتمد | `docs/vision/00-vision-registry.md` هو المصدر الملزم |
| D-002 | الثوابت التسعة شروط قبول ملزمة (لا إرشادات) | معتمد | تُفحص على كل تغيير؛ خرقها يوقف الدمج |
| D-003 | LBD-03 Unified Activity معتمد بستة ملفات تعريف | معتمد | العقد يُصمَّم للستة، ويُفعَّل `mission` فقط في P1 |
| D-004 | Data Residency: لا اختيار مزوّد الآن؛ ويُثبَّت كمتطلب معماري | معتمد | بيانات المستخدم الحساسة في منطقة مناسبة للسوق السعودي · مراعاة PDPL · التحقق من شروط معالجة البيانات وتوثيقه **شرط** قبل أي قرار استضافة في Technical Design |
| D-005 | حجز `username` منذ Phase 1 | معتمد | يُطلب عند التسجيل؛ `/u/{username}` محجوز وغير منشور حتى P4 |
| D-006 | B2B2C احتمال جدّي → `org_id` من Phase 1 | معتمد | موجود في الأساس، غير ظاهر في تجربة المستخدم، ولا لوحة مؤسسات الآن |
| D-007 | Personal Career Companion (A01) يُرفع إلى `THIN` في Phase 1 | معتمد | جزء من هوية المنتج؛ يعمل **فوق** بيانات النظام وقرارات Orchestrator، ولا يخطط من الصفر |
| D-008 | Project / Evidence Hub (C04) يُرفع إلى `THIN` في Phase 1 | معتمد | لإثبات سلسلة: Project → Progress → Evidence → Skill → Achievement → CV/LinkedIn/Case Study |
| D-009 | سياسة AI داخل الأنشطة قائمة على الإفصاح لا المنع المطلق | معتمد | ثلاثة أنماط: `ai_prohibited` · `ai_assisted` · `ai_expected`؛ ولا يُعتمد تصريح المستخدم وحده |
| D-010 | عقد Learning & Development Agent (A07) يُعرَّف الآن رغم تنفيذه الجزئي | معتمد | سلسلة Gap → Need → Resource → Practice → Submission → Evaluation → Evidence |
| D-011 | لا Stack ولا Database ولا LLM provider ولا SRS ولا UI في هذه المرحلة | معتمد | تُحسم في Technical Design بعد اعتماد هذه المرحلة |

## الدفعة الثانية (اعتماد مرحلة الرحلة والعقود والنطاق)

| # | القرار | الحالة | الأثر |
|---|---|---|---|
| D-012 | **قاعدة الدليلين** للمهارة الأساسية | معتمد | دليل واحد ⇒ `Demonstrated` كحد أقصى · دليلان مستقلان من نشاطين ⇒ `Verified` عند استيفاء باقي الشروط |
| D-012a | **مسار الاستثناء الموثّق** | معتمد | نشاط واحد عالي القوة + `platform_controlled` + **مراجعة بشرية إلزامية** + استيفاء كل شروط `Verified` ⇒ يجوز منح `Verified` استثناءً، مع تسجيل **سبب الاستثناء والمراجع**. **ليس المسار الافتراضي**، ويُراقَب معدله. |
| D-013 | **Integrity Checks إلزامية** | معتمد | ترفع من `SHOULD` إلى `MUST` لأي نشاط يمكن أن ينتج `Verified`. التسمية المعمارية **`Integrity Checks`** لا "مصائد". تشمل: تناقضات مدروسة · معلومات ناقصة · outliers · افتراضات يجب اكتشافها · أسئلة دفاع سياقية. **الغاية قياس الفهم والتحقق والاستقلالية، لا خداع المستخدم.** |
| D-014 | **تدرّج ميزانية Companion** | معتمد | ثلاثة أنماط: `Full AI` → `Low-cost AI` → `Deterministic`. **لا يتوقف Companion أبدًا.** بعد السقف يبقى قادرًا على: الحالة الحالية · الخطوة التالية · التقدّم · التذكيرات · التوصيات المخزّنة · شرح القرارات القائمة. التحليل الجديد عالي التكلفة يخضع لسياسة الميزانية. |
| D-015 | **الوضع الآمن عند عدم الحسم = المستوى الأدنى** | معتمد | مع **UX غير عقابي إلزامي**: يُشرح دائمًا الشرط غير المتحقق · سبب عدم الترقية · المطلوب للمستوى التالي · النشاط المقترح · إمكانية طلب مراجعة بشرية إن كانت متاحة |
| D-016 | **الثابت التاسع: `Activity Score ≠ Skill Verification Level`** | معتمد | درجة النشاط **لا تمنح مستوى مهارة تلقائيًا**. مستوى التحقق يعتمد على: بنود الرُبريك · جودة الدليل · قوة المصدر · Integrity Checks · اتساق إقرار AI · بوابات التحقق · الحداثة · عدد الأدلة المطلوبة. **درجة عالية في نشاط ≠ `Verified`.** |
| D-017 | **سلّم المهارات معتمد** | معتمد | `Observed` (داخلي فقط) · `Practiced` · `Demonstrated` · `Verified` |
| D-018 | **تصنيف قوة المصدر معتمد** | معتمد | `platform_controlled` ⇒ سقف `Verified` · `platform_observed` ⇒ سقف `Verified` · `self_reported` ⇒ سقف `Demonstrated` · `third_party_asserted` ⇒ `Demonstrated` حاليًا |
| D-018a | **Validation Activity قصيرة** | معتمد | طريق ترقية دائم وواضح من `self_reported` إلى `platform_observed` — **بلا مطالبة المستخدم بإعادة المشروع كاملًا** |
| D-019 | **سياسة AI معتمدة** | معتمد | `ai_prohibited` · `ai_assisted` · `ai_expected` · الإفصاح لا يخفض الدرجة بذاته · نقيّم: الحكم · التحقق · الاختيار · التبرير · اكتشاف الخطأ · القرار |

## الدفعة الثالثة (اعتماد مرحلة Data Model + Evaluation Architecture)

| # | القرار | الحالة | الأثر |
|---|---|---|---|
| D-020 | **`system_derived`** صنف مصدر خامس رسمي | معتمد | للحقائق الناتجة حتميًا من قواعد النظام (`Gap` · `Claim.level` · أهلية `Achievement` · حسابات الحالة الحتمية). يحمل إلزامًا: `derivation_rule_id/version` · `source_entity_refs[]` · `calculation_timestamp`. **يُمنع استخدامه لأي مخرَج LLM.** |
| D-021 | **`human_override_up`** | معتمد | المراجع البشري هو الطرف الوحيد الذي يرفع المستوى. حقول إلزامية: `reviewer_id` · `reason` · `linked_evidence` · `rubric_version` · `previous_level` · `new_level` · `timestamp` · `audit_event`. **لا يجوز تجاوز شرط جوهري مفقود** (لا دليل · لا نشاط صالح · لا رُبريك · فشل فحص حتمي). الغاية: **تصحيح حكم النظام لا إنشاء إثبات من العدم.** |
| D-022 | **10% = `design_review_threshold`** لا hard cap | معتمد | تجاوزها يُنشئ Design Review لمراجعة `ActivitySpec`s والرُبريكات وقواعد التحقق وقاعدة الدليلين. **ولا تُرفض حالة صحيحة لمجرد تجاوز السقف الإحصائي.** |
| D-023 | **`recency_policy` لكل `Skill` أو `SkillFamily`** | معتمد | حقولها: `current_window` · `aging_window` · `stale_after` · `refresh_method` · `policy_version`. 12/24 شهرًا **قيم ابتدائية لبعض الفئات فقط**. يجب دعم: سريعة التغيّر · مستقرة نسبيًا · سلوكية · تنظيمية/منهجية — **بلا افتراض تساوي التقادم**. |
| D-024 | **مدد الاحتفاظ `provisional product defaults`** | معتمد | إشارات العملية 12 شهرًا · المحادثات 6 أشهر — **مبدئية، غير نهائية، ولا تُ hardcode**. `RetentionPolicy` قابلة للتهيئة لكل فئة بـ: الغرض · المدة أو قاعدة الحساب · سلوك الحذف/إخفاء الهوية · التعليق القانوني · سلوك التصدير. **فصل صريح** بين: محتوى المستخدم · محادثات Companion · إشارات العملية · سجلات الأمن/التدقيق · سجلات الأدلة · سجلات الامتثال · سجلات أنشطة المعالجة. **لا مدة نهائية قبل Privacy/Legal Review سابقة للإطلاق.** |
| D-025 | **مواءمة PDPL للاحتفاظ** | معتمد | تُشتق كل سياسة من: الغرض من المعالجة · أقل مدة لازمة · الالتزام النظامي الخاص · الإتلاف أو إخفاء الهوية بعد انتهاء الغرض عند انطباقه. **ولا يُفترض أن لكل الفئات مدة موحّدة.** |

## الدفعة الرابعة (اعتماد Blueprint + إذن بدء الـSRS)

| # | القرار | الحالة | الأثر |
|---|---|---|---|
| D-026 | **تنفيذ Phase 0** | معتمد | يتحقق من: إكمال الأنشطة · جودة التقييم · اتفاق AI مع المراجع البشري · قيمة `Verified Evidence` لدى مسؤولي التوظيف · الاستعداد للدفع · تكلفة إنتاج النشاط · تكلفة AI لكل مستخدم ولكل دليل. **ويجري بالتوازي مع كتابة الـSRS الوظيفي، ولا يمنعها.** |
| D-027 | **`NAQLA is multi-discipline by design`** *(الصياغة الأصلية: `Career OS is multi-discipline by design` — أُعيدت تسميتها بـ`CHG-005` بلا تغيير دلالي)* | معتمد — **قرار هوية** | `Data Analyst / BI` **مسار تحقق أول فقط (validation beachhead)، وليس هوية المنتج**. **يُمنع** على أي Requirement أو Entity أو Agent Contract أو State Machine أو Activity Model أو Evidence Model أن يفترض أن المنتج خاص بتحليل البيانات. |
| D-028 | **تسجيل المسارات التقنية المستقبلية** | معتمد | 23 مسارًا في 6 عائلات بحالة `REGISTERED` (سجل الرؤية §هـ). **ليست نطاق تنفيذ Phase 1** — الغرض اختبار خلوّ المعمارية من افتراضات المسار الواحد. |
| D-029 | **متطلب قابلية إضافة مسار جديد** | معتمد — **شرط معماري ملزم** | إضافة Track تعتمد على: Role Definition · Skill Map · Activity Specs · Rubrics · Integrity Checks · Learning Resources · Content configuration. **ويُمنع** أن تتطلب إعادة بناء: Career Graph · Evidence model · Claim lifecycle · Evaluation pipeline · Agent orchestration contracts · Project/WorkItem model · CV/LinkedIn/Portfolio projection architecture. **احتياج المسار الثاني لتغيير جوهري في أيٍّ منها = Architecture Failure يُصحَّح قبل التوسع.** |
| D-030 | **ملكية المحتوى في Phase 0/P1** | معتمد | 3 أنشطة فقط لمسار Data/BI · **كل نشاط يمر بمراجعة Subject Matter Expert قبل اعتباره صالحًا لتوليد `Verified Evidence`** · الرُبريكات وIntegrity Checks تخضع للمراجعة نفسها · **يُقاس وقت بناء النشاط فعليًا** ولا تُعتمد التقديرات الحالية كحقيقة. |
| D-031 | **المراجعة البشرية = معايرة لا ميزة** | معتمد | مراجعة كثيفة في البداية · **أول 100 تسليم نافذة معايرة مبدئية** · لا تُخفَّض النسبة إلا بعد ثبات: `agreement rate` · `integrity performance` · `evaluator consistency` · `dispute rate` وفق المعايير المعتمدة. |
| D-032 | **اتجاه الإيراد** | معتمد | **B2C واجهة الاختبار الأولى** · **B2B2C مسار تجاري استراتيجي وجاد** · `org_id` والبنية المؤسسية محفوظة من Phase 1 · **لا Institutional Dashboard الآن**. |
| D-033 | **لا تثبيت لسعر الاشتراك في الـSRS** | معتمد | يُكتب `TBD — validated through Phase 0 pricing experiment`، ويُربط لاحقًا بـ: cost per active user · cost per completed activity · cost per Verified Evidence · willingness to pay. |
| D-034 | **Employer Validation ليست حاجبًا للـSRS** | معتمد | لكنها **Validation Gate لقيمة `Verified`**. **يُمنع تسويق `Verified` بوصفه معترفًا به من أصحاب العمل قبل وجود دليل ميداني.** |
| D-035 | **فصل تصنيف المزايا** | معتمد | الـSRS يفصل صراحة بين: User-Facing Functional Features · Internal Platform Capabilities · Agentic Capabilities · Governance/Control Capabilities. **ولا تُعرض 91 بندًا كقائمة مزايا للمستخدم.** |
| D-036 | **تقييد Platform Admin** | معتمد صراحة | **لا يستطيع:** رفع أو خفض Skill Level · تعديل Evaluation Score · إنشاء Evidence · حذف Evidence لتغيير نتيجة · تجاوز Rubric · منح `Verified`. وقراءة عمل المستخدم تتطلب سبب دعم موثّقًا وحدث تدقيق. |
| D-037 | **قرارات لا تمنع الـSRS الوظيفي** | معتمد | اختيار المزوّدين · أرقام الأداء النهائية · مدد الاحتفاظ النهائية · السعر النهائي · البنية التحتية · مزوّد النماذج — تُكتب **`TBD` صريحة بدل الافتراض**. |

## الدفعة الخامسة (إغلاق مرحلة المتطلبات)

| # | القرار | الحالة | الأثر |
|---|---|---|---|
| D-038 | **نموذج الفريق الأولي** — إغلاق جزئي لـOPEN-005 | معتمد | R-1 Product Owner/Lead Developer (المؤسِّسة) · R-2 AI Development Assistance (**ليست عضو فريق ولا جهة اعتماد**) · R-3 SME للـData/BI (**اعتماده شرط حاجب لنشر أي نشاط يمنح `Verified`**) · R-4 Human Reviewer (**يجوز أن يكون نفس الـSME في Phase 0** بفصل مسجَّل بين مراجعة المحتوى ومراجعة التسليم، مع `role_performed` إلزامي). **الأرقام المالية والزمنية تبقى `TBD` وتُقاس في Phase 0.** التفاصيل: `docs/ops/27-team-resource-model.md` |
| D-039 | **مصفوفة تتبّع تفصيلية إلزامية** | معتمد | تتبّع أمامي وعكسي · `TC-TBD-xxx` كعناصر نائبة · **كل Requirement بلا مصدر وكل Feature بلا Requirement يظهر كـGap صريح ولا يُخفى**. التفاصيل: `docs/srs/SRS-001-traceability-matrix.md` |
| D-040 | **اختبار المعمارية متعدد التخصصات** | معتمد | ARCH-REQ-TRACK-05 يُشترط فيه أن يكون المسار الثاني **من عائلة مختلفة جذريًا عن Data/BI**. المرشحان: **Frontend Developer (TRK-01)** أو **IT / Application Support (TRK-11/12)**. **الهدف Architecture Validation لا إطلاق المسار للمستخدمين.** فشل الاختبار ⇒ إصلاح المعمارية قبل أي توسع. |
| D-041 | **مبدأ تعقيد التجربة** | معتمد | **`Internal complexity must not become user complexity.`** وجود 19 وكيلًا وعشرات القدرات ومحركات الحوكمة والتقييم **لا يعني أن المستخدم يتعامل معها**. واجهة المستخدم المستقبلية تتمحور حول سبعة أسئلة، و**`Personal Career Companion` هو الواجهة الحوارية الأساسية** التي تنسّق مع الخبراء خلف الكواليس. **مبدأ مسجَّل — لا تصميم UI الآن.** |
| D-042 | **تجميد الأساس الوظيفي** | معتمد | `SRS-001` v1.1 بحالة **`FUNCTIONAL BASELINE APPROVED`**. أي تغيير لاحق في متطلب معتمد يتطلب: `Change ID` · السبب · الأثر · تحديث مصفوفة التتبّع. **ولا يُعدَّل الأساس الوظيفي بصمت.** قيم `TBD` **ليست قرارات نهائية**. |

## الدفعة السادسة (تطبيق ضبط التغيير)

| # | القرار | الحالة | الأثر |
|---|---|---|---|
| D-043 | **اعتماد وتطبيق `CHG-001..004`** | معتمد ومطبَّق | **`CHG-001`** `FR-P-036` اشتقاق `Achievement` حتميًا · **`CHG-002`** `FR-P-037` واجهة مراجعة عمياء وسجل قرار غير قابل للتعديل يشمل `role_performed` · **`CHG-003`** `F-AI-11` · `F-GOV-12` · `F-GOV-13` في الجرد · **`CHG-004`** `F-CON-12` بوابة اعتماد SME. **المبرر: سدّ فجوات تتبّع لقدرات معتمدة أصلًا — لا توسعة نطاق.** نطاق Phase 1 لم يتغير. `SRS-001` → **v1.2** بحالة `FUNCTIONAL BASELINE APPROVED`. |
| D-043a | **استثناء موثّق على قاعدة التجميد** | معتمد | القاعدة تشترط إخراج بند مكافئ عند إضافة `MUST`. **استُثنيت الإضافات الأربع** لأنها توثيق لمتطلبات معتمدة سلفًا (D-030 · D-035 · D-036) ولا تضيف عملًا جديدًا ولا تغيّر تعريف اكتمال Phase 1. |
| D-043b | **الأرقام تُحسب ولا تُقدَّر** | معتمد | بعد اكتشاف خطأي عدّ (GAP-00 · GAP-07)، **كل عدّ في الوثائق يُشتق آليًا من الصفوف نفسها**، ويُعلَن قاعدة الاشتقاق حين يكون التصنيف اجتهاديًا. |

## الدفعة السابعة (اعتماد اسم المنتج — `CHG-005`)

| # | القرار | الحالة | الأثر |
|---|---|---|---|
| D-044 | **اسم المنتج الرسمي: `نَقْلة` / `NAQLA`** — إغلاق `OPEN-019` | معتمد — **قرار هوية** | الشعار النصي: **«من المهارة إلى الدليل. / From skill to evidence.»** · **«Career OS» اسم عامل متقاعد** لا يُستخدم في أي نص جديد ولا في أي واجهة · العربي **بالتشكيل دائمًا** (`نَقْلة`) واللاتيني `NAQLA` بأحرف كبيرة وتباعد `0.18em` في الشعار النصي فقط · **داخل التطبيق العلامة وحدها**، والاسم في: المصادقة · onboarding · الإعدادات › عن · شاشة البدء · تذييل دراسة الحالة العامة · الأصول المُصدَّرة. التفاصيل: `docs/changes/CHG-005-product-name.md` |
| D-044a | **سياسة إعادة التسمية: الأسماء تتغيّر، المُعرِّفات لا** | معتمد | ✅ تتغيّر: العناوين والنصوص المقروءة · ⛔ **لا تتغيّر**: `SRS-001` · `D-0xx` · `CHG-0xx` · `OPEN-0xx` · `FR-x-0xx` · `F-xxx-0x` · `CAP-xx` · `TRK-xx` · `INV-x` · `ARCH-REQ-TRACK-0x` · **ولا أسماء الملفات** (منها `docs/srs/SRS-001-career-os-functional.md`). **السبب: تغيير المُعرِّف يكسر كل تتبّع سابق بلا مقابل.** الاسم داخل مسار الملف **وصف لا هوية**. |
| D-044b | **إغلاق `OPEN-019` لا يغلق ما جاوره** | معتمد | `OPEN-016` (روابط دراسات الحالة العامة) و`OPEN-017` (تبريد `username`) **يبقيان مفتوحين** · التسجيل التجاري للعلامة واسم النطاق **خارج سجل المتطلبات**، ولا تعتمد عليهما أي وثيقة. |

## الدفعة الثامنة (قرار الـStack والانتقال إلى Production Phase 0)

> §21 قاعدة ٢: ملء قيمة `TBD` **لا يحتاج `CHG`**، بل تسجيلًا في سجل القرارات. وهذه الدفعة كذلك — **لا متطلب تغيّر، ولا نطاق اتّسع**.

| # | القرار | الحالة | الأثر |
|---|---|---|---|
| D-045 | **الـStack الإنتاجي** — إغلاق `OPEN-021` | معتمد | **الواجهة:** Next.js + TypeScript + App Router · **الـAPI:** NestJS + TypeScript + REST مبدئيًا · **البيانات:** Supabase PostgreSQL · **المصادقة:** Supabase Auth · **التخزين:** Supabase Storage · **المهام الخلفية:** طابور فوق PostgreSQL **مع إبقاء التجريد قابلًا للاستبدال** · **النشر:** الويب على Vercel، والـAPI بحاوية مستقلة عن المزوّد. |
| D-045a | **لا قرار استضافة غير قابل للرجوع للـAPI في Phase 0** | معتمد | يُبنى كصورة حاوية واحدة تعمل على أي مضيف. **و`OPEN-022` يبقى حاجبًا على الإطلاق** (لا على البناء): توثيق موطن البيانات **يسبق** اختيار المزوّد (D-004). ولذلك `NAQLA_DEPLOY_REGION` **متغيّر إلزامي** يرفض الـAPI الإقلاع بدونه. |
| D-046 | **`packages/domain` مستقل عن الإطارين — قاعدة معمارية ملزمة** | معتمد | يحوي القواعد المرجعية: حالات الأدلة والمهارات · الانتقالات · نتائج التقييم · حساب الجاهزية · قواعد النزاهة · قواعد إفصاح AI · الادعاء المدعوم وغير المدعوم · أهلية تقرير مسؤول التوظيف · أهلية السيرة ولينكدإن · أهلية الخصوصية والمشاركة · **الثوابت التسعة**. **يُمنع** على Next.js أو NestJS إعادة تعريف أيٍّ منها؛ يستهلكانها. **مفروض آليًا** بـ`scripts/verify-boundaries.mjs`: النطاق لا يستورد إلا `node:` builtins، وإعلان أي اسم قاعدة داخل تطبيق **يفشل البناء**. |
| D-046a | **الثوابت تُفرض في قاعدة البيانات لا في التطبيق** | معتمد | INV-1 مفتاح أجنبي وقيد، لا شرط برمجي. **٢٩ إثباتًا** في `supabase/tests/invariants.sql` كلٌّ منها محاولة كتابة يجب أن ترفضها القاعدة. **قاعدة PR: ثابت بلا قيد قاعدة = ثابت غير مفروض.** |
| D-047 | **`apps/web/` نموذج مُجمَّد ومرجع، لا قاعدة كود إنتاجية** | معتمد | يُستعمل مرجعًا بصريًا ومحتوى وتفاعلًا، **وأساسَ مقارنة انحدار** للتجاوب وRTL. **يُمنع** أن يصير تدريجيًا التطبيق الإنتاجي، **ويُمنع** حذفه أو إعادة كتابته. مفروض بـ`scripts/verify-frozen-prototype.mjs` وبيان `FROZEN-MANIFEST.json`. |
| D-048 | **أوزان الدرجات تبقى `TBD` صريحة** | معتمد | `loadWeights()` **يفشل بصوت مسموع** ما لم تُعتمد الأوزان. **يُمنع** وضع قيم اعتباطية معقولة المظهر لأنها تجعل نموذجًا غير مُعاير يبدو محسومًا (امتداد D-037). **مفاتيح المكوّنات** معتمدة (من §9 من التصميم)؛ **الأوزان لا**. |
| D-049 | **لا استدعاءات نماذج ولا مفاتيح مزوّدين في Phase 0** | معتمد | `OPEN-023` (شروط معالجة البيانات) حاجب. **واختبار في `@naqla/config` يفشل** إن أُضيف مفتاح مزوّد إلى مخطط البيئة. التجريد عقد مكتوب في `AI-BOUNDARY.md`، **ولم يُنفَّذ**. |
| D-050 | **حفظ مفردتَي السلّم بدل دمجهما** | معتمد | سجلات المنتج (`Observed`) والتصميم (`gap` · `self_reported`) **كلاهما محفوظ**. `gap` **رتبة عرض لا صف مخزَّن** (قيد `claim_gap_is_not_stored`). والفرق بينهما ليس تجميليًا: `self_reported` الموجودة في سيرة المستخدم **تُوسَم ادعاءً غير مدعوم**، و`gap` لا شيء يُوسَم. **كشف هذا الفرقَ اختبارُ وحدة سقط عند محاولة الدمج.** |
