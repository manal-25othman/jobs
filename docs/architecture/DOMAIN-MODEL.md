# نَقْلة / NAQLA — Domain Model
**الإصدار:** 0.1.0 · **التاريخ:** 2026-09-26 · **الحالة:** مسوّدة للمراجعة الداخلية — Phase 0

> **مصدر الحقيقة للقواعد هو `packages/domain`.** هذه الوثيقة تشرحه ولا تحل محلّه.
> عند أي اختلاف بين الوثيقة والكود، **الكود هو الصحيح** والوثيقة تُصحَّح.

---

## ٠. المبادئ الحاكمة لهذا النموذج

1. **لا حقل لأن الواجهة تحتاجه.** كل حقل يعود إلى متطلب أو ثابت أو ضرورة تشغيلية، ومذكور بجانبه.
2. **الثوابت في القاعدة لا في التطبيق.** ما يمكن فرضه بقيد `CHECK` أو مفتاح أجنبي يُفرض هناك؛ فالقاعدة لا تنسى.
3. **التاريخ لا يُعاد كتابته.** الجداول التي تحمل تاريخًا تُضاف إليها الصفوف ولا تُعدَّل.
4. **الغياب ليس حالة مخزَّنة.** `gap` مثلًا رتبة عرض، لا صف في جدول.
5. **الخصوصية تصنيف على الكيان، لا إعداد يُنسى.** كل كيان يحمل `privacy classification`.

### تصنيف الخصوصية المستخدم في الجداول أدناه

| التصنيف | المعنى |
|---|---|
| `private_never_shareable` | لا يغادر الحساب بأي مسار، مهما كانت الإعدادات |
| `private_shareable_derived` | خاص، ويجوز مشاركة **مشتق معتمد** منه فقط |
| `private_publishable` | خاص افتراضيًا، ويجوز نشره كما هو **بعد اعتماد صريح** |
| `non_personal` | محتوى منصة بلا بيانات شخصية |

### ملاحظة على السلّم: مفرداتان محفوظتان

سجلات المنتج (`docs/model/10`) تسمّي السلّم:
`Observed → Practiced → Demonstrated → Verified` (+ `Aging` · `Historical`).
التصميم المُجمَّد (§8) يسمّيه:
`Gap → Self-reported → Practiced → Demonstrated → Verified`.

**كلاهما محفوظ ولم يُدمج.** التصميم يشقّ `Observed` إلى حالتين مرئيتين للمستخدم:
`gap` (الدور يطلبها ولم يفعل شيئًا) و`self_reported` (أعلنها بلا مشروع).
داخليًا كلتاهما `Observed`: لا تُعرض علنًا (D-017)، ولا تبلغ `demonstrated` بلا تسليم مُقيَّم.
**والفرق بينهما ليس تجميليًا**: مهارة `self_reported` موجودة في سيرة المستخدم **تُوسَم كادعاء غير مدعوم**، بينما `gap` لا شيء يُوسَم.
هذا الفرق كشفه اختبار وحدة سقط عند محاولة دمجهما، وهو مثبَّت الآن في `presentationFor()`.

---

## ١. `User` — المستخدم

| | |
|---|---|
| **الغرض** | صاحب الحساب والعمل والأدلة. جذر كل ملكية. |
| **المُعرِّف** | `app_user.id` (uuid) — **يساوي `auth.users.id`** في Supabase |
| **الملكية** | نفسه |
| **دورة الحياة** | `created → active → (soft-deleted) → purged after retention` |
| **الخصوصية** | `private_never_shareable` (باستثناء `username` و`display_name` عند نشر ملف عام) |

**الحقول:**

| الحقل | لماذا يوجد | قابل للتعديل؟ |
|---|---|---|
| `id` | مرآة لهوية Supabase. **فُصل الملف عن المصادقة** حتى لا يُعيد تغيير مزوّد الهوية كتابة بيانات المنتج | ⛔ ثابت |
| `username` | بذرة الملف العام والرابط (D-005) | ✅ مع قيود |
| `display_name` | العرض في الواجهة | ✅ |
| `locale` | `ar` أو `en` — يقود الاتجاه والتنسيق (§5) | ✅ |
| `org_id` | **محفوظ من Phase 1** لمسار B2B2C (D-032). لا ميزة مؤسسية اليوم | ✅ |
| `deleted_at` | حذف ناعم: الحساب قابل للاسترجاع، وسجل التدقيق يجب أن يبقى خلال مهلة الحذف (D-024 · D-025) | ✅ |

**العلاقات:** يملك `CareerGoal` · `Project` · `Submission` · `Evidence` · `SkillClaim` · `ProfessionalAsset` · `CvVersion` · `Notification` · `Consent` · `ShareLink` · `PublicProfile`.

> **`username_release`**: جدول منفصل يسجّل تحرير اسم المستخدم عند الحذف. `OPEN-017` (مدة التبريد) **لم يُحسم**، وتسجيل وقت التحرير الآن يعني إمكانية تطبيق السياسة لاحقًا بلا إعادة بناء التاريخ.

---

## ٢. `Career Goal` — الهدف المهني

| | |
|---|---|
| **الغرض** | الدور الذي يقيس المستخدم نفسه مقابله. مرجع كل فجوة وكل توصية. |
| **المُعرِّف** | `career_goal.id` |
| **الملكية** | المستخدم |
| **دورة الحياة** | `confirmed → current → superseded` — **تغيير الهدف يحتفظ بالتاريخ ولا يمحوه** |
| **الخصوصية** | `private_shareable_derived` |

| الحقل | لماذا | قابل للتعديل؟ |
|---|---|---|
| `target_role_id` | الدور المرجعي | ⛔ (التغيير = هدف جديد) |
| `confirmed_at` | خطوة **«هذا هو هدفي المهني الأساسي الآن»** في onboarding — تأكيد صريح لا اختيار عابر | ⛔ |
| `is_current` | فهرس فريد جزئي يضمن **هدفًا حاليًا واحدًا فقط** | ✅ |

---

## ٣. `Target Role` — الدور المستهدف

| | |
|---|---|
| **الغرض** | سجل `role_definition`: اسم، مهارات مطلوبة بأوزان، حالة مراجعة، وسم مصدر. |
| **المُعرِّف** | `target_role.id` · `slug` فريد |
| **الملكية** | المنصة (محتوى مُنسَّق) |
| **دورة الحياة** | `draft → in_review → published → deprecated` |
| **الخصوصية** | `non_personal` |

**قاعدة ملزمة (§1 من التصميم):** إن كان تعريف الدور ناقصًا تُعرض «**البيانات غير مكتملة بعد**» — **ولا تُخترع متطلبات**. لذلك `review_status` حقل أول لا حقل ثانوي.

**ولا شيء في هذا الكيان خاص بـFrontend.** نفس البنية تخدم Data Analyst وBI وBackend وUX وأي مسار (D-027 · D-029).

---

## ٤. `Role Requirement` — متطلب الدور

| | |
|---|---|
| **الغرض** | يربط دورًا بمهارة بوزن، ويحمل الطلب السوقي عليها. |
| **المُعرِّف** | `role_requirement.id` · فريد على (`target_role_id`, `skill_id`) |
| **الخصوصية** | `non_personal` |

| الحقل | لماذا |
|---|---|
| `weight` | يقود ترتيب الفجوات. `0 < weight <= 1` |
| `is_core` | يحدد أين تنطبق **قاعدة الدليلين** — `OPEN-012` ما زال مفتوحًا على تعريف «المهارة الأساسية» |
| `demand_ratio` · `demand_source` · `demand_observed_at` | **INV-4**: «مطلوبة في ٧ من ١٠ إعلانات» معلومة سوقية، وقيد `role_requirement_demand_sourced` يرفض نسبة بلا مصدر وتاريخ |

---

## ٥. `Skill` — المهارة

| | |
|---|---|
| **الغرض** | هوية المهارة عبر كل المسارات. |
| **المُعرِّف** | `skill.id` · `slug` فريد |
| **الملكية** | المنصة |
| **الخصوصية** | `non_personal` |

**قرار بنيوي (LBD-08): المهارات عالمية لا خاصة بمسار.** لو كانت لكل مسار نسخته من «اختبار الواجهات» لانكسرت هوية المهارة عبر المسارات، ولصار الانتقال بين المسارات فقدانًا للدليل.

---

## ٦. `Skill Requirement` — متطلب المهارة

مُجسَّد في `role_requirement` أعلاه ولا يحتاج جدولًا ثالثًا: «متطلب المهارة» هو الوزن والأساسية والطلب في سياق دور بعينه. **فصله كان سيُنتج جدولًا بحقل واحد وعلاقتين.**

---

## ٧. `Project` — المشروع

| | |
|---|---|
| **الغرض** | وعاء العمل الذي ينتج الدليل. نوعان: نشاط منصة ومشروع شخصي. |
| **المُعرِّف** | `project.id` |
| **الملكية** | المستخدم |
| **دورة الحياة** | `draft → active → completed → evidenced` · جانبية: `archived` · `withdrawn` |
| **الخصوصية** | `private_publishable` |

| الحقل | لماذا | قابل للتعديل؟ |
|---|---|---|
| `kind` | `platform_activity` أو `personal_project` — يحدد قوة المصدر وسقف الدرجة | ⛔ |
| `activity_spec_id` + `activity_spec_version` | **INV-7**: قيد `project_activity_has_spec` يرفض نشاط منصة بلا مواصفة منشورة | ⛔ بعد البدء |
| `deleted_at` | حذف ناعم: `withdrawn` تُخفي ولا تحذف، والدليل يجب ألّا يتدلّى بلا مصدر | ✅ |

---

## ٨. `Project Evidence` — دليل المشروع

مُجسَّد بعلاقة `evidence.project_id` وجدول `asset_evidence`. **ليس كيانًا ثالثًا** لأن «دليل المشروع» ليس شيئًا مختلفًا عن `Evidence`: هو `Evidence` مصدره مشروع. إفراده كان سيُنتج مسارين لنفس الحقيقة، وهو بالضبط ما يخلق تناقضًا لاحقًا.

---

## ٩. `Evidence` — الدليل

> **جوهر نَقْلة.** كل ما عداه مشتق منه أو يقود إليه.

| | |
|---|---|
| **الغرض** | واقعة مُثبتة تربط مستخدمًا بمهارة، بمصدرها وقوتها وثقتها. |
| **المُعرِّف** | `evidence.id` |
| **الملكية** | المستخدم — **والمستخدم لا يكتبه**: ينشأ من خط التقييم عبر service role |
| **دورة الحياة** | `created → (withdrawn)` — لا تعديل، ولا حذف |
| **الخصوصية** | `private_shareable_derived` — يُعرض مشتقه (بند سيرة، دراسة حالة)، لا هو |

| الحقل | لماذا | قابل للتعديل؟ |
|---|---|---|
| `skill_id` | أي مهارة يُثبتها | ⛔ |
| `source_strength` | **يسقف الدرجة**: `self_reported` و`third_party_asserted` يسقفان عند `demonstrated`؛ `verified` يحتاج `platform_*` | ⛔ |
| `evaluation_result_id` | قيد `evidence_platform_needs_evaluation` يرفض دليلًا بقوة منصة بلا تقييم خلفه | ⛔ |
| `provenance_class` + `provenance_source` | **INV-5** | ⛔ |
| `confidence` | ثقة مستقلة عن الدرجة — **ثلاثة أبعاد منفصلة**: المستوى، وقوة المصدر، والثقة | ⛔ |
| `withdrawn_at` + `withdrawn_reason` | السحب لا يحذف. وقيد يرفض سحبًا بلا سبب مكتوب. **وسحب الدليل يسحب ما اشتُق منه** (`FR-P-036`) | ✅ مرة واحدة |

---

## ١٠. `Evidence Source` — مصدر الدليل

قيمة على `Evidence` (`source_strength`)، لا جدول. القيم الأربع مُعدّدة في القاعدة (`evidence_source_strength`) فلا تُدخَل قيمة خارجها أصلًا.

| القيمة | السقف |
|---|---|
| `platform_controlled` | `verified` |
| `platform_observed` | `verified` |
| `third_party_asserted` | `demonstrated` |
| `self_reported` | `demonstrated` |

---

## ١١. `Evidence State` — حالة الدليل

| | |
|---|---|
| **الغرض** | موقع المهارة على السلّم، كما يراه المستخدم. |
| **المُعرِّف** | نوع مُعدَّد `evidence_state`، ورتبته `evidence_ordinal()` |
| **التخزين** | على `skill_claim.state` |

**`gap` ليست صفًّا.** قيد `claim_gap_is_not_stored` يرفض تخزينها: الفجوة هي **غياب** ادعاء، وتُحسب بالفرق بين متطلبات الدور والادعاءات الموجودة. تخزينها كان سيعني صفوفًا لكل مهارة لم يلمسها المستخدم قط، ثم سؤالًا لا إجابة له: متى تُنشأ؟

**المقارنة بالرتبة لا بالنص.** الواجهة لا تستنتج الاتجاه من ترتيب الحروف (§8)، والقاعدة تفرض ذلك بـ`evidence_ordinal(to) > evidence_ordinal(from)`.

---

## ١٢. `Evaluation` — التقييم

| | |
|---|---|
| **الغرض** | تشغيلة تقييم على تسليم واحد. |
| **المُعرِّف** | `evaluation.id` |
| **دورة الحياة** | `queued → running → completed` · جانبية: `failed → queued_for_human` · `disputed → revised` |
| **الخصوصية** | `private_never_shareable` (النتيجة تُعرض، لا التشغيلة) |

**التقييم ليس حقل `status` على المشروع.** هذا صريح في طلب المالكة، ومفروض بنيويًا: لا عمود `evaluation_status` على `project` إطلاقًا.

---

## ١٣. `Evaluation Result` — نتيجة التقييم

| | |
|---|---|
| **الغرض** | حدث تاريخي **غير قابل للتعديل** يحمل نتيجة تشغيلة. |
| **المُعرِّف** | `evaluation_result.id` |
| **دورة الحياة** | **إضافة فقط.** إعادة التقييم **تُضيف صفًّا** يشير إلى سابقه بـ`supersedes_result_id` |
| **الخصوصية** | `private_shareable_derived` |

**الحالات الخمس** (`evaluation_outcome`):

| الحالة | المعنى | المصدر |
|---|---|---|
| `passed` | بلغ العتبة؛ يجوز اشتقاق دليل | doc 11 §5–§6 |
| `below_threshold` | عمل حقيقي مُقيَّم دون العتبة. **«لا دليل» نتيجة مشروعة** وتُعرض بوضوح | doc 11 §6 |
| `blocked_by_checks` | فشل فحص حتمي إلزامي؛ يتوقف الخط وتُتاح فرصة تصحيح واحدة | doc 11 §3 |
| `undetermined` | انقطاع نموذج النزاهة. الوضع الآمن: المستوى الأدنى + مراجعة بشرية. **ولا درجة مُخمَّنة أبدًا** | doc 11 §4 |
| `needs_human_review` | فشل المُقيِّم بعد إعادة محاولة واحدة، أو تصعيد | doc 11 §5 · §9 |

> ✅ **`OPEN-EVAL-01` مُغلق بقرار المالكة (D-051): المفهومان يبقيان منفصلين.**
>
> `EvaluationOutcome` يجيب: **ماذا حدث أثناء محاولة التقييم؟**
> `VerificationOutcome` يجيب: **ماذا قرّرت خطوة تحقق مستقلة أو بشرية؟**
>
> **ولا يُدمجان** في تعداد واحد ولا عمود حالة واحد: تقييم جرى بلا خلل قد ترفضه المراجعة، ومراجعة قد تُصعَّد بينما التقييم `passed`. دمجهما يجعل «انتهت الآلة» و«وافق إنسان» شيئًا واحدًا — وهو الفرق الذي يقوم عليه سلّم `Verified` كله.
>
> **العلاقة والتعدّدية:**
> ```
> Submission ─1:N→ Evaluation ─1:N→ EvaluationResult ─0:1→ Verification
>             (محاولات)          (تاريخ لا يُعدَّل)      (قرار عن نتيجة)
> ```
> · **1:N للمحاولة** — يجوز إعادة تقييم تسليم (اعتراض، إعادة محاولة).
> · **1:N للنتيجة** — إعادة التقييم **تُضيف** صفًّا مرتبطًا بـ`supersedes_result_id`.
> · **0:1 للتحقق** — ليست كل نتيجة تُتحقَّق، **وقرار واحد لكل نتيجة**: قراران على نتيجة واحدة التباس بلا قراءة صحيحة.
>
> **دورتا الحياة** منفصلتان تمامًا في `packages/domain/src/verification.ts`:
> `Evaluation`: `queued → running → completed | failed → queued_for_human · completed → disputed → revised`
> `Verification`: `pending → evaluating → accepted | downgraded | rejected | escalated_to_human → human_resolved | exception_granted`
>
> **وقاعدة مطلقة على التحقق:** يخفض أو يثبّت، **ولا يرفع أبدًا** — مفروضة بقيد `verification_never_raises`. و`downgraded` **تثبّت عند الحالة الحالية** ولا تُنزل المستخدم، لأن v1 لا يُنمذج التراجع أصلًا.

| الحقل | لماذا | قابل للتعديل؟ |
|---|---|---|
| `rubric_version_id` | **INV-2** — قيد `evaluation_result_scored_needs_rubric` | ⛔ |
| `activity_spec_id` + `activity_spec_version` | **INV-7** — نفس القيد | ⛔ |
| `supersedes_result_id` | فهرس فريد: **نتيجة واحدة لا تُستبدَل مرتين** | ⛔ |
| `human_reviewer_id` + `role_performed` | `FR-P-037` · D-038 — **من** راجع، و**بأي صفة**، منفصلان لأن الـSME والمراجع قد يكونان شخصًا واحدًا في Phase 0 | ⛔ |

---

## ١٤. `Evaluation Criterion` — بند التقييم

| | |
|---|---|
| **الغرض** | درجة بند واحد داخل نتيجة، مع مبرره ومقتطفه. |
| **المُعرِّف** | `evaluation_criterion_score.id` · فريد على (`result`, `criterion_key`) |
| **دورة الحياة** | إضافة فقط، مع النتيجة |

**قيدان يحرسان جودة التقييم:**
- `rationale` **غير فارغ إلزاميًا** — درجة بلا مبرر مكتوب ليست نتيجة (doc 11 §5).
- `criterion_score_in_range` — `0 <= score <= max_score`.

`supporting_excerpt` يجعل الدرجة **قابلة للمراجعة**: مراجع بشري يرى على أي سطر بُنيت.

---

## ١٥. `Integrity Check` — فحص النزاهة

| | |
|---|---|
| **الغرض** | فحص سلامة على التسليم (D-013، وسُمّي «Integrity Check» بدل «trap»). |
| **المُعرِّف** | `integrity_check.id` |
| **الخصوصية** | `private_never_shareable` |

**`classification` حقل حاكم:** `user_facing` يجوز شرحه للمستخدم، و`assessment_only` **لا يُعرض ولا يُشارك بأي مسار** — لأن عرض الفحص يعلّم كيف يُجتاز. وRLS لا تمنح أي سياسة قراءة لهذا الجدول لأي عميل، **وهذا مُختبَر**.

---

## ١٦. `AI Disclosure` — إقرار استخدام الذكاء الاصطناعي

| | |
|---|---|
| **الغرض** | ما يقوله المستخدم عن استعانته بالذكاء الاصطناعي في تسليم بعينه. |
| **المُعرِّف** | `ai_disclosure.id` · **واحد لكل تسليم** |
| **دورة الحياة** | يُسجَّل مع التسليم، ولا يُعدَّل بعده |
| **الخصوصية** | `private_never_shareable` |

**أهم ما في هذا الجدول ليس فيه: لا عمود عقوبة.**
سياسة الاستخدام تقول إن الإفصاح **لا يخفض الدرجة بذاته**، وحقل غير موجود لا يُساء استخدامه لاحقًا. الأوضاع الثلاثة مُعدَّدة، وقيد `ai_disclosure_prohibited_means_none` يرفض إقرار تأليف على نشاط `ai_prohibited`.

إشارات التثليث (I1–I6) وسلّم N0–N4 **مُنفَّذة في `packages/domain/src/ai-disclosure.ts`**، ونوع الإرجاع نفسه يمنع العقوبة: `scorePenaltyApplied: false` قيمة حرفية لا متغيّر.

---

## ١٧. `Professional Asset` — الأصل المهني

| | |
|---|---|
| **الغرض** | مخرج قابل للعرض مشتق من دليل: بند سيرة · مهارة لينكدإن · دراسة حالة · مشروع للعرض. |
| **المُعرِّف** | `professional_asset.id` |
| **دورة الحياة** | `generated → user_edited → approved → published → unpublished` |
| **الخصوصية** | `private_publishable` |

| الحقل | لماذا |
|---|---|
| `user_approved_at` | **قيد `asset_published_needs_approval`**: لا نشر بلا اعتماد. ولا تُطبَّق صياغة مهنية يولّدها النظام دون اعتماد صريح (§9) |
| `drafting_aid_used` | نموذج ساعد في **الصياغة**، والجوهر من الدليل. **معونة صياغة ليست حقيقة مولّدة** |
| `provenance_class` | INV-5 |
| `asset_evidence` | جدول ربط: أي دليل أنتجه. **فارغ يعني أصلًا غير مدعوم بدليل**، وهو ما تمنعه قواعد العرض |

**قاعدة من آلات الحالات (§10):** `user_edited` خارج الدليل **يُسقط وسم التوثيق**. الأصل لا يبقى «موثّقًا» بعد أن يُعاد كتابته بما يتجاوز ما أثبته التقييم.

---

## ١٨. `CV Version` — نسخة السيرة

| | |
|---|---|
| **الغرض** | لقطة سيرة في زمن، بلغة واحدة، لمقارنة «قبل/بعد». |
| **المُعرِّف** | `cv_version.id` · فريد على (`user`, `version_number`, `language`) |
| **دورة الحياة** | إضافة فقط |
| **الخصوصية** | `private_shareable_derived` |

**المستند نفسه في التخزين لا في الصف** (`document_bucket` + `document_path`). و`content_snapshot` من نوع `jsonb` **بتبرير مكتوب**: لقطة زمنية للمقارنة، لا حالة نطاق يُستعلم عنها.

---

## ١٩. `CV Assessment` و ٢٠. `LinkedIn Assessment`

| | |
|---|---|
| **الغرض** | ربط تقييم ملف بنسخة الدرجة التي أنتجته. |
| **المُعرِّف** | `cv_assessment.id` · `linkedin_assessment.id` |
| **الخصوصية** | `private_shareable_derived` |

كلاهما يشير إلى `readiness_score_id` **إلزاميًا**: التقييم ليس رأيًا حرًّا، بل قراءة درجة محسوبة بمكوّناتها.

`linkedin_assessment.sections` من نوع `jsonb` **بتبرير**: أسماء أقسام لينكدإن الثمانية عقد منصة خارجية **لا نملكها**، وتثبيتها أعمدةً يجعل تغييرًا عندهم هجرةً عندنا.

---

## ٢١. `Case Study` — دراسة الحالة

| | |
|---|---|
| **الغرض** | سرد من أربعة أقسام ثابتة، مكتوب **من مشروع مُقيَّم فقط**. |
| **المُعرِّف** | `case_study.id` · مرتبط بأصل واحد |
| **الخصوصية** | `private_publishable` |

**الأقسام الأربعة أعمدة لا `jsonb`**: «السياق · المشكلة · ما بنيتُه · النتيجة» **قاعدة منتج** لا شكل مرن. لو كانت `jsonb` لأمكن نشر دراسة حالة بثلاثة أقسام، والقاعدة تمنع ذلك.

`source_evaluation_result_id` **إلزامي**: تُكتب من تقييم، لا من وصف ذاتي.

---

## ٢٢. `Recruiter Report` — تقرير مسؤول التوظيف

| | |
|---|---|
| **الغرض** | مراجعة بعين مسؤول توظيف: ٣–٤ ملاحظات، كل منها مع سببها والدرجة التي تحركها. |
| **المُعرِّف** | `recruiter_report.id` |
| **دورة الحياة** | إضافة فقط — كل توليد نسخة |
| **الخصوصية** | `private_shareable_derived` |

**`traces_to_kind` + `traces_to_ref` إلزاميان على كل بند** — **INV-5** وقاعدة §9 الصريحة: «كل توصية تشير إلى معلومة أو دليل في حسابك، لا إلى تقدير عام». بند بلا إشارة **يُرفض عند الإدخال**.

و`recruiter_report_ineligible_has_reason`: تقرير غير مؤهل **يحمل سبب عدم أهليته**. «لا يوجد تقرير» بلا تفسير حالة فارغة رديئة.

---

## ٢٣. `Public Professional Profile` — الملف المهني العام

| | |
|---|---|
| **الغرض** | صفحة عامة تتغذّى من الأصول المعتمدة. |
| **المُعرِّف** | `public_profile.id` · `slug` فريد |
| **دورة الحياة** | `private → published → unpublished` |
| **الخصوصية** | `private_publishable` |

**مُختبَر:** نشر الملف العام **لا يكشف** الأدلة خلفه ولا الملفات المرفوعة. سياسة `anon` تمنح قراءة `public_profile` وحده.

---

## ٢٤. `Share Link` — رابط المشاركة

| | |
|---|---|
| **الغرض** | فتح باب واحد إلى مورد واحد، مؤقتًا وقابلًا للإلغاء. |
| **المُعرِّف** | `share_link.id` · `token_hash` فريد |
| **دورة الحياة** | `created → (revoked | expired)` |
| **الخصوصية** | `private_never_shareable` (الرابط نفسه سر) |

**`token_hash` لا الرمز نفسه**: تسريب قاعدة البيانات لا يسلّم روابط عاملة.

**`expires_at` إلزامي و`indexable` مقيَّد بـ`false`** — لأن `OPEN-016` (فهرسة · انتهاء · إلغاء) **لم يُحسم**، والقراءة المتحفظة هي الافتراض حتى يُحسم. هذا قيد قاعدة بيانات، لا اتفاق شفهي.

**دورة الحياة مُختبَرة بالكامل**: بلا رابط ⇒ مغلق · برابط حي ⇒ مفتوح · بعد الإلغاء ⇒ مغلق فورًا · بعد الانتهاء ⇒ مغلق.

---

## ٢٥. `Learning Gap` — فجوة التعلّم

| | |
|---|---|
| **الغرض** | مهارة يطلبها الدور ولا دليل عليها، مرتّبة بالأثر. |
| **المُعرِّف** | `learning_gap.id` |
| **الملكية** | المستخدم |
| **الخصوصية** | `private_shareable_derived` |

**`provenance_class` افتراضه `system_derived`**: الفجوة **محسوبة** من متطلبات الدور والادعاءات، لا مُدخلة يدويًا ولا مولّدة بنموذج.

> **تنبيه لغوي مثبت في التصميم (§8):** العنوان في الواجهة «أهم ثلاث نقاط تحتاج تقدّمًا» — **ولا يُسمّى «فجوات»** إن كانت القائمة تضم `practiced` أو `self_reported`. كلمة «فجوة» محجوزة لحالة `gap` وحدها.

---

## ٢٦. `Learning Resource` — مصدر التعلّم

| | |
|---|---|
| **الغرض** | مرجع خارجي مرتبط بمهارة. |
| **المُعرِّف** | `learning_resource.id` |
| **الخصوصية** | `non_personal` |

**`verification_status` افتراضه `source_to_verify`**، وقيد `learning_resource_verified_has_url` يرفض وسم «مُتحقَّق» بلا رابط فعلي. **لا تُخترع روابط** — هذه قاعدة من حزم البيانات، وهي الآن مفروضة في القاعدة لا في مراجعة بشرية.

---

## ٢٧. `Practice Activity` — نشاط التدريب

| | |
|---|---|
| **الغرض** | تدريب أو تعلّم أكمله المستخدم. |
| **المُعرِّف** | `practice_activity.id` |
| **الخصوصية** | `private_never_shareable` |

**قاعدة صريحة (§8): إكمال التعلّم والتدريب لا يغيّر حالة دليل إطلاقًا.**
لذلك لا يوجد أي مفتاح من هذا الجدول إلى `evidence` أو `skill_claim` أو `evidence_transition`. **العلاقة الغائبة هي القاعدة.**

---

## ٢٨. `Notification` — التنبيه

| | |
|---|---|
| **الغرض** | إخطار المستخدم بما تغيّر. |
| **المُعرِّف** | `notification.id` |
| **دورة الحياة** | `created → read → dismissed` |
| **الخصوصية** | `private_never_shareable` |

`read_at` و`dismissed_at` منفصلان لأن §10 يشترط **تذكّر حالة الإخفاء لكل حدث**. و`type` مُقيَّد بالعقد اللوني المُجمَّد: `success` · `improvement` · `attention` · `info` · `critical`.

---

## ٢٩. `Audit Event` — حدث التدقيق

| | |
|---|---|
| **الغرض** | **INV-8**: كل فعل ذي معنى يُنشر كحدث. |
| **المُعرِّف** | `audit_event.id` |
| **دورة الحياة** | **إضافة فقط.** `UPDATE` و`DELETE` **مسحوبتان** لا مُهمَلتين |
| **الخصوصية** | `private_never_shareable` |

**هذه بيانات منتج لا قياس تشغيلي.** تخضع لسياسة الاحتفاظ وحق التصدير والتعليق القانوني، **ولا تُخلط بسجلات التشغيل أبدًا** — خلطهما يضع دليل المستخدم تحت سياسة حذف أداة مراقبة.

| الحقل | لماذا |
|---|---|
| `reason` **غير فارغ إلزاميًا** | «لا انتقال صامت، ولا انتقال بلا سبب مُسجَّل» |
| `actor_kind` + `role_performed` | قيد يرفض مراجعًا بشريًا بلا صفة مُسجَّلة |
| `payload` (`jsonb`) | **بتبرير**: أشكال الأحداث تختلف، والحقول القابلة للاستعلام **رُقِّيت أعمدة** |

---

## كيانات إضافية لزمت ولم تُطلب

| الكيان | لماذا كان لا بد منه |
|---|---|
| `Submission` · `SubmissionFile` | التسليم **يُقفل** ثم لا يُعدَّل؛ التصحيح تسليم جديد مرتبط. بلا هذا الكيان يصير «الدليل» بلا مادة |
| `HumanReview` | `FR-P-037`: مراجعة **عمياء** وسجل قرار غير قابل للتعديل |
| `SkillClaim` | «الحالة الحالية لمهارة» ≠ «الدليل». الدليل واقعة، والادعاء خلاصة الوقائع |
| `EvidenceTransition` | سجل السلّم. بلا سجل إضافة-فقط، الانتقال يصير تعديل عمود بلا تاريخ |
| `ActivitySpec` · `RubricVersion` | INV-2 · INV-7 يحتاجان نسخة منشورة **مجمّدة** يُشار إليها |
| `ModelCall` | INV-6: **استدعاء بلا صف هنا لم يحدث** |
| `MarketFact` | INV-4 في مكان واحد بدل تكراره في كل جدول يحمل رقمًا سوقيًا |
| `Consent` · `ReadinessScore` · `ScoreComponent` · `Job` | الموافقة إضافة-فقط · الدرجة بمكوّناتها ومحرّكها · الطابور المعاملاتي |

---

## ما لم يُنمذَج عمدًا

| البند | لماذا |
|---|---|
| **التراجع في السلّم** (`downgrade` · `revoke`) | ليس في v1.0. وقيد `transition_is_forward` يرفضه. إضافته لاحقًا **حالة مراجعة صريحة موسومة**، لا أثر جانبي لنشاط |
| **أوزان الدرجات** | `loadWeights()` **يفشل بصوت مسموع** حتى تُعتمد. رقم معقول المظهر يجعل نموذجًا غير مُعاير يبدو محسومًا |
| **جلسات الرفيق المهني** | لا استدعاءات نماذج في Phase 0، و`OPEN-023` مفتوح |
| **الملف العام كمحتوى** | `OPEN-016` مفتوح؛ بُني الهيكل والباب مغلق |
| **لوحة المؤسسات** | D-032: `org_id` محفوظ، ولا ميزة مؤسسية الآن |
