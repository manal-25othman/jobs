# نَقْلة / NAQLA — Vertical Slice 01.1 · Production Hardening Report
**الإصدار:** 1.0 · **التاريخ:** 2026-09-26 · **الحالة:** بانتظار اعتماد المالكة

> **الحقيقة الحاكمة لهذا التقرير:** بيئة البناء **لا تحوي بيانات اعتماد Supabase**، **وسياستها الشبكية ترفض `supabase.com`** (`connect_rejected` في سجل الوكيل). لذلك قسم «PROVEN ON REAL SUPABASE» **فارغ**، ولم أملأه بادعاء. كل ما يعتمد على مشروع حيّ بُني كاملًا، وأُثبت محليًا بما يُثبت محليًا، **وسكربت إثبات حيّ من ١٢ خطوة جاهز لتشغيلك** حين تتوفر البيئة.

---

# قرارات المالكة — مطبَّقة أولًا

| # | القرار | التنفيذ | الإثبات |
|---|---|---|---|
| 1 | `معاينة → تعديل اختياري → اعتماد صريح → فعّال` دائمًا | مسودة لم تُعايَن **لا تُعتمد**: النطاق يرفض `draft → approved`، والـAPI يعيد `422 missing_prerequisite`، والقاعدة ترفض `active` بلا اعتماد (D-057) | اختبار سلبي e2e + اختبار نطاق |
| 2 | لا نسب تقدّم مختلقة | `deliverableProgress()` يعطي «N من M مخرجات إلزامية مكتملة» — **ونوعه بلا حقل نسبة أصلًا**. `persistedPercentage()` يرفض مصدرًا غير محفوظ مُسمّى (D-058). الواجهة تعرض العدّ لا النسبة | اختبارا نطاق |
| 3 | `Verified` محجوب، `Demonstrated` السقف | `assertStateAvailableInProduction` قبل كل ترقية + **قيد `transition_verified_not_yet_available`** في القاعدة؛ إزالته هجرة مُراجَعة لا مسار كود (D-059) | e2e: إدراج `verified` بمراجع مسمّى **ترفضه القاعدة** |

---

# PROVEN ON REAL SUPABASE

**لا شيء.** لا بيانات اعتماد، ولا وصول شبكي إلى Supabase من هذه البيئة.

ما يلزم لنقل البنود من القسم التالي إلى هنا: متغيرات `NEXT_PUBLIC_SUPABASE_URL` · `NEXT_PUBLIC_SUPABASE_ANON_KEY` · `SUPABASE_SERVICE_ROLE_KEY` · `SUPABASE_JWT_SECRET` · `DATABASE_URL` في بيئة التشغيل (لا في الدردشة)، وإضافة `supabase.com` و`*.supabase.co` إلى النطاقات المسموحة، ثم:
```bash
supabase db push && psql "$DATABASE_URL" -f supabase/seed/0001_demo_role.sql
npm run start -w @naqla/api
NAQLA_API_URL=http://localhost:3001 npm run live:supabase
```

---

# PROVEN LOCALLY ONLY

> **٢١٩ فحصًا، صفر فشل**: نطاق ٩٩ · إعدادات ٨ · ثوابت القاعدة ٢٩ · وصول RLS ٤٢ · طرف-إلى-طرف **٤١** (٢٩ الشريحة الأولى + **١٢ حماية**). قاعدة بيانات PostgreSQL 16 حقيقية؛ **التخزين بديل اختبار ذاكري موسوم** (D-060) يُرفض عند `NODE_ENV=production` (مُختبَر).

| المطلوب | ما أُثبت محليًا | حدّ الإثبات |
|---|---|---|
| **Supabase Auth** | الحارس الإنتاجي يتحقق من توقيع HS256 ومطالبة `sub` كما يصدرها Supabase؛ رمز مزوّر ⇒ `401`؛ بلا سرّ مضبوط **يرفض الـAPI مصادقة أي أحد** بدل التراجع إلى فكّ غير موقَّع | الرموز مُصدَرة محليًا بنفس السرّ والشكل. **إصدار Supabase نفسه (تسجيل، بريد، تجديد) غير مُثبت** |
| **الجلسة وتجديد الرمز** | الواجهة تجدّد الرمز إجباريًا حين يتبقى < ٦٠ث (`accessToken()`)، و`autoRefreshToken` مفعّل | **لم يُشغَّل ضد Supabase**؛ الخطوة ٤ في السكربت الحيّ تثبته (تجديد ⇒ رمز جديد يقبله الـAPI) |
| **RLS** | ٤٩/٤٩ جدولًا بـ`enable`+`force`، ٥٥ سياسة، ٤٢ إثباتًا بأدوار `authenticated` و`anon` حقيقية، بحارس يفشل إن لم يتبدّل الدور | على PostgreSQL محلي بأدوار Supabase مُنشأة عند غيابها. **على Supabase الحقيقي غير مُنفَّذ** — الخطوة ٦ في السكربت |
| **تخزين خاص + توقيع فقط** | نية ⇒ صف `pending` + هدف `PUT` موقّع ٦٠ث · تأكيد يقرأ الكائن المخزَّن ويقيس الحجم و`sha256` (**القيمة المصرَّح بها لا تُصدَّق**، مُختبَر) · تنزيل موقّع ٣٠٠ث للمالك فقط · **لا استجابة API تحمل مسار كائن أو حاوية أو بادئة مالك** (مُختبَر على النية والتأكيد والتنزيل) | محوّل Supabase Storage **مكتوب كاملًا** (`createSignedUploadUrl` · `createSignedUrl` · `download` · `list`) **ولم يُنفَّذ** |
| **مصدر الملف وملكيته** | جدول `upload` بـ`provenance_class = user_generated`، مسار مسبوق بمعرّف المالك (قيد)، `privacy_class` مقيَّد بـ`never_shareable` (قيد)، تأكيد بلا قياس مرفوض (قيد) | — |
| **تسليم يشير إلى ملفات و/أو روابط** | `uploadIds[]` (مؤكَّدة ومملوكة فقط) + `externalUrls[]` (http/https فقط، بلا بيانات اعتماد)؛ ملف نصّي حرّ مرفوض (`400`)، `ftp://` مرفوض (`422`) | — |
| **مستخدم ثانٍ لا يصل ملفات غيره** | من الـAPI: تنزيل وتأكيد وإشارة في تسليم ⇒ `404` · من القاعدة بدور `authenticated`: **صفر صفوف**، وإدراج مرفوض، وتعديل **يُصفّى إلى صفر صفوف** والاسم لم يتغيّر | Storage نفسه (قائمة/توقيع من المستخدم الآخر) — الخطوة ٩ في السكربت |
| **Recruiter View لا يكشف ملفًا خامًا** | بوجود ملف **حقيقي في التخزين**: التقرير الخاص والإسقاط العام خاليان من `object_path` · `bucket` · اسم الملف · `checksum` · رابط موقّع · `upload` · بادئة المالك · والرابط الخارجي | مفاتيح ممنوعة أُضيفت إلى `REPORT_FORBIDDEN_KEYS` وتُفحص على أي عمق |
| **روابط مشاركة قابلة للإلغاء** | `DELETE /v1/share-links/:id`: يُغلق المسار العام **فورًا** · مستخدم آخر لا يلغي (`400` ولا يتغيّر شيء) · إلغاء ثانٍ مرفوض · زر إلغاء في الواجهة | «ضد البيئة الحقيقية» — الخطوة ١٢ في السكربت (بما فيه فحص RLS بمفتاح `anon` قبل الإلغاء وبعده) |
| **إعادة تشغيل المجموعات الموجودة** | كلها أُعيدت وتمرّ. تغيّر واحد: فيكسشرات e2e صارت **رفعًا حقيقيًا** (نية ← `PUT` ← تأكيد) لأن الملف النصّي الحرّ لم يعد مقبولًا | — |

**تحسين جانبي لزم:** مرشّح استثناءات يحوّل رفض قاعدة نطاق إلى `4xx` **باسم القاعدة** (`invariant_violation` · `illegal_transition` · `missing_prerequisite`) بدل `500` — مستخدم يحاول اعتماد مسودة لم يُعاينها **ليس حادثة تشغيل**.

---

# BLOCKED

| # | البند | السبب | يفتحه |
|---|---|---|---|
| 1 | كل بنود «PROVEN ON REAL SUPABASE» | لا بيانات اعتماد + الشبكة ترفض `supabase.com` | متغيرات البيئة + السماح بالنطاق، ثم `npm run live:supabase` |
| 2 | Supabase محليًا | لا خدمة Docker | بيئة بها Docker أو مشروع حيّ |
| 3 | `OPEN-022` موطن البيانات | يمنع الإطلاق لا البناء | التوثيق قبل اختيار المزوّد (D-004) |
| 4 | `OPEN-023` شروط مزوّد النماذج | يمنع أي AI | لا AI في هذه الشريحة أصلًا |
| 5 | `Verified` | بقرارك (D-059) | اعتماد SME + سير التحقق المستقل — **إزالة قيد واحد مُسمّى** |

---

# NOT IMPLEMENTED

| البند | لماذا |
|---|---|
| AI · وكلاء · `Verified` · شاشات أخرى · CV/LinkedIn | خارج النطاق بقرارك؛ **لم يُلمس شيء منها** |
| فحص البرمجيات الخبيثة على المرفوعات | ليس في نطاق هذه الشريحة؛ نوع المحتوى مقيَّد بقائمة مغلقة والحجم بسقف (١٠ MiB) فقط |
| تنظيف المرفوعات المعلّقة (`pending` بلا تأكيد) | يحتاج مهمة خلفية؛ الطابور غير مستعمل بعد. الصفوف المعلّقة **غير ضارة** (لا كائن خلفها) |
| رفع السيرة (`cv_upload`) | الحاوية والغرض معرَّفان؛ لا واجهة له — خارج النطاق |
| ربط `submission_file` القديم بـ`upload` | الجدول من Phase 0 باقٍ بلا استعمال؛ يُدمج أو يُحذف في هجرة لاحقة **بقرار** |

---

# ما تغيّر في الأرقام

`+١` جدول (`upload`) · `+٦` قيود · `+١` سياسة · **٢٣ نقطة نهاية** (+٤: نية الرفع، التأكيد، التنزيل، إلغاء الرابط) · `DOMAIN_RULESET_VERSION 0.1.0 → 0.2.0`.

# أخطاء وقعتُ فيها هنا

| # | الخطأ | التصحيح |
|---|---|---|
| 1 | مساعد الاختبار `expectRejected` كان يعالج **نجاح** البيان خطأً (يحرّر savepoint ثم يحاول التراجع إليه) | أُصلح؛ وكشف أن `UPDATE` غير المسموح **لا يُرفض** تحت RLS بل يُصفّى إلى صفر صفوف — فصار الإثبات «صفر صفوف والاسم لم يتغيّر»، وهو الإثبات الصحيح |
| 2 | `$1` بنوعين في SQL اختبار | تحويل صريح |

**توقفت هنا. لم أتجاوز نطاق الحماية.**
